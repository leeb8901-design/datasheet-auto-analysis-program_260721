# 작업 인계 문서 (Handoff) — 2026-07-30

> 이 문서는 오늘 정리·확정한 내용을 **Claude Code에서 이어서 작업**하기 위한 인계용입니다.
> 프로젝트: 데이터시트 자동분석 → PTC Windchill(MIL-HDBK-217F Notice 2) 신뢰도 파라미터 생성.
> 저장소: `datasheet-auto-analysis-program_260721`

---

## 0. 핵심 원칙 (반드시 유지)

- **엑셀 = 원본(single source of truth), JSON = 생성물.** 엑셀을 고치면 `tools/build_reference.py`를 다시 돌려 `data/*.json`을 재생성한다. 값을 코드에 직접 손으로 적지 않는다.
- 분석 프로그램은 실행 시 **로컬 JSON만** 읽고 **AI(LLM)는 실행 중 개입하지 않는다**(규칙 기반 + 사람 검토).
- 규칙 기반 추출로 **비어 있는 값만** 참고표로 자동 채우고, 자동 판정값은 근거를 남겨 **사람이 검토**한다.
- 매핑맵 색상 규칙: 흰=직접입력 / 회=N/A / 보라=PTC자동 / 분홍=데이터시트로 알수없음. **LibreOffice 재저장 금지**(색상이 통째로 사라지는 사고 이력 있음).

---

## 1. 오늘 산출물 (파일)

| 파일 | 내용 |
|---|---|
| `Data_list_217F_수정.xlsx` | 입력지(원본). 아래 3·4번 수정 반영본. GitHub의 Import_입력지를 대체 예정. |
| `tools/build_reference.py` | 엑셀 → `data/*.json` 변환 스크립트(헤더 앵커 기반 파싱 + 검증 출력). |
| `data/quality_by_temp.json` | 카테고리 × 동작온도범위 → 품질등급. |
| `data/thermal_resistance.json` | IC/반도체 θJC(접합-케이스) 기본값. |
| `data/env_conversion.json` | 환경 변환 팩터(11개 환경). |
| `data/temp_conversion.json` | 온도 변환 팩터(수정 완료본). |
| `data/quality_allowed.json` | 서브카테고리별 허용 Quality Level + `commercial_fixed` 플래그. |
| `ai/reference.py` | 위 JSON 로더 + 판정 함수(품질/θJC/MTBF). |
| `ai/pdf_parser.py` | `analyze_pdf`에 참고표 자동판정 연결(수정본). |
| `repo_patch.zip` | 위 신규/수정 파일을 저장소 상대경로 구조로 묶음. 루트에서 압축 해제. |

---

## 2. 확정된 규칙 / 결정사항

### 2-1. 품질등급 = 동작 온도 범위 기반 (방향 B)
데이터시트의 동작 온도 범위로 품질등급을 정한다. (넓은 범위 → 좋은 등급 → 낮은 πQ. 낙관 편향은 감수하기로 사용자가 결정.)

| 카테고리 | 0~70℃ | -40~85℃ | -55~125℃ | No Data |
|---|---|---|---|---|
| Integrated Circuit | Commercial | B-1 | B | Commercial |
| Semiconductor | Commercial | JAN | JANTX | Commercial |
| Resistor | Commercial | M | P | Commercial |
| Capacitor | Commercial | L | M | Commercial |
| Inductor - Coil | Commercial | Mil-Spec | P | Commercial |
| Inductor - Transformer | Commercial | Mil-Spec | Mil-Spec | Commercial |
| Relay | Commercial | Mil-Spec | P | Commercial |
| Connection / Switching / Misc | Commercial | Mil-Spec | Mil-Spec | Commercial |
| Optical Device | Commercial | JAN | JANTX | Commercial |

### 2-2. 규칙2 — 서브카테고리 미허용 → Commercial 고정
위 등급이 그 **서브카테고리의 허용 Quality Level 목록에 없으면 Commercial로 고정**한다.
- 해당 항목(18개)은 `유효성 목록` 시트에서 **노란색(fill FFE699)** + 셀 코멘트 + 좌상단 범례로 표시됨.
- 예: 가변/트리머 커패시터(CT/CB/CV 등)는 {Mil-Spec, Commercial}만 허용 → L·M 미허용 → Commercial.
- `data/quality_allowed.json`의 `commercial_fixed:true` 로 코드에서 판정.

### 2-3. θJC(접합-케이스 열저항) 기본값 — 데이터시트에 없을 때 폴백
`TJ = TC + θJC × P`. 스펙값과 표값 중 **큰 쪽** 사용, 아무것도 없으면 반도체 **기본 70**. 표에 없는 패키지(최신 SMD 등)는 자동입력하지 말고 "확인 필요" 표시.
- **마이크로회로(217F §5.11):** Dual-In-Line=28, Flat Package=22, Chip Carrier=20, Pin Grid Array=20, Can=70 (다이면적 ≤14,400 mil² 기준).
- **반도체(217F Table 6-2):** [5] TO-28,53,57,59,60,61,63,83,94,126,127,220, DO-4,5,8,9,14,45,205AB / [10] TO-3,41,66,204,204AA, DO-7,13,29,35,41 / [22] TO-89 / [70] TO-1,5,8,9,12,18,33,39,44,46,52,71,72,92,99,205AD,205AF, DO-204MB, PA/PD/PT/PH/PY 계열.
  - (TO-126=5, TO-127=5, DO-4=5, DO-8=5, TO-204=10 은 사용자 확인 반영분.)

### 2-4. 온도 변환표 수정 (중요)
기존 입력지의 온도 변환 팩터 표가 **환경 변환표 값을 복사한 오류**였음(온도 오르면 MTBF 증가하는 역방향). → 아레니우스 방향(온도↑ → MTBF↓)으로 교체 완료. `data/temp_conversion.json` 이 수정본.
- 반올림 규칙: 표에 없는 온도는 가장 가까운 칸으로, **동점이면 높은 쪽**(예: 25 → 30).

### 2-5. MTBF 변환 공식 & 주석 양식
제조사 MTBF 제공 시: **적용 MTBF = 데이터시트 MTBF × 환경변환팩터 × 온도변환팩터**.
주석 양식(결과값 포함):
```
[데이터시트 환경], [온도]도 환경
MTBF : [값]

[적용 환경], [온도]도 환경
MTBF : [값] * [환경팩터] * [온도팩터] = [적용 MTBF]
```
검증 예: GB25→GB40 = 850,000×1×0.9 = **765,000** / GB25→NS40 = ×0.3×0.9 = **229,500** / GB25→NS25 = ×0.3×1 = **255,000**.

---

## 3. 코드 반영 현황 (오늘 완료)

`ai/reference.py` — `data/*.json`을 읽어 아래를 제공:
- `resolve_quality_level(category, subcategory, temp_bucket)` → (등급, commercial_fixed)
- `parse_temp_range(text)` / `classify_temp_range(min,max)` → 온도버킷
- `find_package_in_text(category, text)` / `resolve_thermal_resistance(category, package, spec_value)` → (θJC, 근거)
- `env_factor` / `temp_factor` / `convert_mtbf(...)`

`ai/pdf_parser.py` — `analyze_pdf`가 분석 후 **빈 값일 때만** Quality Level·Thermal Resistance(θJC)를 참고표로 채우고, 근거를 `reference_notes`로 반환(사람 검토용). 기존 추출은 덮어쓰지 않음.

> 오늘 이 자리에서 임포트/판정/변환 테스트 통과 확인함(값이 엑셀과 일치).

---

## 4. 남은 작업 (Claude Code TODO)

1. **UI ↔ 분석 ↔ 출력지 연결**: `analyze_pdf`(참고표 사용까지 됨)를 `ui/main_window.py`의 결과 표시·`excel/output_builder.py`(출력지 writer)까지 연결. (현재 분석 결과가 UI/출력지로 흐르는 배선이 핵심 미완.)
2. **`reference_notes` → "미확인 항목" 컬럼 반영**: 자동 판정값을 출력지 `미확인 항목`(utils/config.py `COL_UNRESOLVED_FIELDS`)에 근거와 함께 남기기.
3. **`subcat_params.json` 이원화 해소**: 기존 154개(매핑템플릿 기반) vs 새 입력지 PSA 97개 — 정본 결정 후 통합. (오늘은 기존 154개를 보존하고 새 것은 넣지 않음.)
4. **θJC vs θJA 정합**: 폴백이 채우는 값은 θJC(접합-케이스)인데 기존 `Temperature Rise = Power × ThermalResistance`는 θJA를 쓰던 자리. 노트에 "θJC" 명시해두었으나, 어느 지표로 통일할지 결정 필요.
5. **실제 PDF로 통합 테스트**: `analyze_pdf`를 샘플 데이터시트로 end-to-end 검증(패키지 탐지·온도범위 파싱 정확도 포함). GUI 실행 확인은 사용자 PC에서 필요.

---

## 5. 주의사항 / 컨벤션

- **자료조사 vs 작업물 분리**: 질문은 기본 자료조사·확인용. "작업물에 반영/적용"이라고 명시할 때만 실제 파일 수정.
- `data/*.json` 로딩은 `ai/prompt.py`의 `DATA_DIR` 패턴을 따른다.
- 보라(PTC 자동결정: Pi Q Value, Case/Junction/Frame Temp Override, Hot Spot Temperature)·분홍(데이터시트로 알수없음: Years in Production, Initial Temp Rise) 칸은 **채우지 않는다**.
- 기존 `subcat_params.json`(154개)을 `build_reference.py`가 덮어쓰지 않도록 주의(현재 스크립트는 `data/`에 생성하므로 실행 전 정본 결정 필요).
- 실행마다 새 출력 파일 생성 방침(원본 덮어쓰기 금지) 유지.

---

## 6. 빠른 시작 (Claude Code)

1. `repo_patch.zip`을 저장소 루트에서 압축 해제.
2. `Data_list_217F_수정.xlsx`를 루트에 두고 `python tools/build_reference.py` 실행 → `data/*.json` 재생성 + 검증 샘플 확인.
3. `python -c "import ai.pdf_parser, ai.reference"` 로 임포트 정상 확인.
4. 위 4번 TODO 중 **1번(UI/출력지 연결)**부터 진행 권장.
