# 작업 인계 문서 (Handoff) — 2026-09-04

> 다른 환경에서 이어서 작업하기 위한 인계용. (이전 문서: `HANDOFF_2026-07-31.md`)
> 프로젝트: 데이터시트 자동분석 → PTC Windchill(MIL-HDBK-217F Notice 2) 신뢰도 파라미터 생성.

---

## 0. 가장 큰 변화

이번 세션은 크게 세 갈래로 진행됨:

1. **분석 정확도 버그 다수 수정** — 실제 부품(9종)을 하나씩 프로그램으로 돌려보며 분류/추출/주석
   오류를 찾아 고침. 판단 근거·규칙은 전부 `CLAUDE.md`에 이미 정리돼 있음(오늘 세션에서 계속
   갱신됨 — **분석 규칙을 알고 싶으면 이 문서 말고 `CLAUDE.md`를 볼 것**, 여긴 "무엇을 했는지"만
   요약).
2. **치명적 버그 2건 수정**(아래 1번 표 참고): PSA 파라미터 시트 읽기 로직이 일부 파라미터를 몰래
   누락하던 버그, PDF 주석이 재실행할 때마다 중복으로 쌓이던 버그.
3. **배포용(Lite) 설치파일 작업** — 사용자 요청으로 **지금은 보류**. 진행 상황과 미해결 이슈는
   6번에 정리. **다음에 이어받으면 이 부분부터 볼 것.**

---

## 1. 오늘 변경한 파일

| 파일 | 변경 |
|---|---|
| `tools/build_reference.py` | **치명적 버그 수정**: `build_subcat_params()`가 행을 읽을 때 첫 빈 셀에서 멈춰서, 서브카테고리마다 공유하는 비연속 컬럼 레이아웃 때문에 뒤쪽 파라미터가 통째로 누락되고 있었음(예: Transistor는 13개 중 4개만 인식). **PSA 입력 파라미터 시트 자체는 안 건드리고** 읽는 코드만 고침(사용자 명시 조건). 97개 서브카테고리 전수 재검증 → 4개 서브카테고리에서 숨은 파라미터 발견, 전부 회복. |
| `datasheet/annotator.py` | **치명적 버그 수정**: `annotate_pdf()`가 `doc.saveIncr()`(추가전용 저장)를 쓰면서 이전 주석을 절대 지우지 않아, 같은 부품을 재분석할 때마다 하이라이트가 중복 누적됨(실제로 12개까지 쌓인 파일 발견). 함수 시작에서 제목이 일치하는 기존 주석을 먼저 지우는 멱등 처리 추가. Category/Subcategory 요약 하이라이트의 앵커 우선순위를 `__category__`→`__subcategory__`로 교체(엉뚱한 위치 강조 수정). Capacitance 주석에 Units를 "동반 필드"로 묶어서 표시. |
| `ai/classifier.py` | 페라이트 비드(`ferrite bead`) 인덕터 키워드 추가. 제목에 `Oscillator`/`Transistor`가 있으면 정상 스코어링을 우회하는 좁은 예외 2건 추가(카테고리 전체에 제목 우선순위를 적용했다가 IS31FL3296이 LED 오탐으로 회귀하는 걸 발견해서 되돌리고, 이 2개 키워드만 남김). 커패시터 서브카테고리를 CSR/CWR/CLR 3지선다에서 **탄탈럼 전용 CSR/CLR 2지선다**(`classify_tantalum_subcategory`)로 교체 — CWR(Chip) 옵션 제거(사용자 확정). 제논/트랜지스터 서브카테고리 키워드에 풀 문구("surface mount zener diode", "middle power transistor" 등) 우선 매칭 추가. |
| `ai/reference.py` | 대량 추가: 페라이트 비드 Type 판정, 발진기 주파수(위상잡음 조건 텍스트에서), Power Rating 크로스라인 폴백, SM 시리즈 Power Rating, 제너 다이오드 Type/Construction Type, 트랜지스터 Vceo(→Rated Voltage)/용도(Linear·Switching) 자동판정, 커패시터 EIA 코드 디코딩(Capacitance/Rated Voltage), 탄탈럼 ESR→Series Resistance 버킷 매핑. |
| `ai/pdf_text.py` | PDF 폰트 디코딩 버그 수정: 일부 벤더 PDF에서 소수점이 U+FFFD(디코딩 불가 문자)로 깨져 나와 "0.15"가 "15"로 읽히던 문제(TC33X-2-102E에서 발견) — 숫자 사이 U+FFFD를 `.`으로 치환하는 후처리를 텍스트 추출 공통 경로에 추가(앞으로 나올 모든 문서에 적용됨). |
| `ai/field_extractor.py` | Power Rating/Operating Power에 W/watt/mW 단위 형태 검증 추가(전압값·각주번호를 잘못 집던 오탐 수정). |
| `ai/pdf_parser.py` | 위 `ai/reference.py` 신규 함수들을 실제 파이프라인에 연결(카테고리/서브카테고리 게이트 체크 후 필드값·근거·주석 기록). |
| `utils/config.py` | **`.env` 위치 안정화**: 프로그램 설치 폴더(`{app}`)가 아니라 OS 표준 사용자별 위치(`%AppData%\DatasheetDownloader\.env`)로 분리. 예전 위치에 있으면 자동으로 새 위치에 복사(마이그레이션). 설치파일이 재배포/이동돼도 API 키가 안 사라짐. |
| `diagnostics/self_check.py` | `.env` 못 찾을 때 에러 메시지에 실제로 확인한 경로 목록을 표시하도록 개선. |
| `installer/set_api_key.ps1`, `installer/launch.ps1`, `installer/빌드방법.md` | 위 `.env` 위치 변경에 맞춰 동기화. |
| `data/headers.json`, `data/subcat_params.json`, `data/package_type_allowed.json`, `data/valid_values.json` | `tools/build_reference.py` 재실행 결과(버그 수정 반영). |
| `vba/Import_User.xlsx`, `vba/Export_Root.xlsx` | 입력/출력 템플릿에 **`분류기준`(분류 판별 기준 설명) 시트** 추가·최신화. |
| (삭제) `vba/Import_양식.xlsm` | 구버전 입력 템플릿(2026-07-31에 `Import_User.xlsx`/`Export_Root.xlsx` 체계로 이미 대체된 것을 오늘 정리). |
| `installer/` (신규 다수) | 배포용 설치파일 빌드 인프라 — 6번 참고. |

---

## 2. 확정된 규칙/결정 (요약 — 전체 근거는 CLAUDE.md)

- **페라이트 비드**: Category=Inductor, Subcategory=Coil, Type=Power Filter (제목의 "CHIP FERRITE BEAD" 등에서 판정). 앞으로 나올 모든 페라이트 비드 부품에 적용되는 일반 규칙.
- **제목에 "Oscillator"가 있으면**: Category=Miscellaneous, Subcategory=Quartz Crystal. Frequency는 위상잡음 조건문(예: "@156.25MHz")에서.
- **제목에 "Transistor"가 있으면**: Category=Semiconductor 확정(다른 키워드 스코어링보다 우선). Rated Voltage=Vceo(콜렉터-이미터 항복전압), Application은 용도 설명에서 Linear/Switching 자동판정.
- **탄탈럼 커패시터**: Solid, Elec, Tant (CSR) / Nonsolid, Elec, Tant (CLR) 중에서만 선택(Chip/CWR 옵션은 폐기). "wet/non-solid" 문구가 없으면 기본 CSR. Capacitance는 Ordering Info의 "Capacitance Code (pF)"(EIA 3자리 코드), Rated Voltage는 "코드 = 값" 리터럴 라인, Series Resistance는 ESR 코드(mΩ)→Ω 환산 후 유효성 목록 버킷.
- **제너 다이오드**: Diode Type=Voltage Regulator, Ref, Zener. Construction Type은 원문에 "spring-loaded" 등 특정 문구 없으면 기본 Metallurgically.
- **PSA 입력 파라미터 시트는 절대 수정하지 않고, 읽는 코드만 고친다**(사용자 명시 원칙 — `tools/build_reference.py` 버그 수정 때 재확인됨).
- 모든 파라미터 값은 `유효성 목록` 시트의 허용값 중에서만 고른다(최우선 원칙, `CLAUDE.md` §0).

---

## 3. 다른 환경에서 셋업

`HANDOFF_2026-07-31.md` §3과 대부분 동일. 추가/변경된 부분만:

1. **`.env` 위치가 바뀜**: 이제 `%AppData%\DatasheetDownloader\.env`에 `MOUSER_API_KEY=...`를 둔다
   (git 미포함, 예전처럼 프로그램 폴더 안이 아님). 예전 위치(`utils/config.py`가 있는 프로젝트
   루트의 `.env`)에 그대로 둬도 자동으로 새 위치로 마이그레이션되긴 하지만, 새로 셋업하는
   환경이면 처음부터 새 위치에 두는 게 깔끔함.
2. 데이터 재생성이 필요하면: `python tools/build_reference.py` → `data/*.json` 갱신(이번에 로직이
   바뀌었으니, 다른 환경에서도 한 번 재생성해서 최신 JSON인지 확인 권장).
3. 나머지(Python 3.14, 실제 Chrome 필요 등)는 `HANDOFF_2026-07-31.md` §3 그대로.

---

## 4. 남은 작업 (TODO)

1. **[중단됨 — 6번 참고] Lite 배포판의 "Invalid unique identifier" Mouser 오류 미해결.** 키/코드는
   문제없음을 확인했으나, 실제 설치 환경에서 Python 런타임 자체가 통째로 사라지는 훨씬 심각한
   문제를 발견함(6번 참고). 사용자가 Windows Defender 보호 기록을 확인하는 걸 기다리는 중.
2. 오늘 고친 8개 부품(IS31FL3296 외) 전체를 실제 GUI로 한 번 더 처음부터 끝까지(다운로드→분석
   →주석→저장) 돌려서 회귀 확인 — 이번 세션엔 대부분 파이썬 함수 직접 호출/부분 재실행으로
   검증했고, GUI 버튼을 눌러서 하는 end-to-end는 IS31FL3296 1건만 함.
3. `installer/lite/README.md`가 안내하는 "개인용 .env 번들 빌드" 절차가 실제로 재현 가능한지
   재확인(스크래치패드에만 있던 `env_bundle/.env`는 세션 종료 시 사라짐 — 다시 만들려면 실제
   `.env`를 그 자리에 복사하는 과정을 사람이 직접 해야 함, README에 이미 안내됨).

---

## 5. 주의사항

- **LibreOffice/recalc 재저장 금지** (색상 소실 사고 이력, `CLAUDE.md` §8).
- `.env`(MOUSER_API_KEY)는 절대 커밋 금지(.gitignore 등록됨) — **단, `installer/lite/setup.iss`에
  개인용 번들 빌드 방법이 주석으로 설명돼 있음(README 포함). 이 방식으로 실제 키를 넣어 빌드한
  Setup.exe는 절대 남에게 공유 금지** — 사용자가 "나만 쓰는 용도"로만 명시적으로 승인한 방식임.
- 작업지/템플릿 엑셀 수정 스크립트는 항상 백업 후 진행.

---

## 6. Lite 배포판 작업 — 보류 상태, 이어받을 내용

**사용자 요청으로 패키징 작업은 여기서 중단하고 다음으로 미룸.** 아래는 진행 상황과 미해결 이슈.

### 진행된 것
- `installer/lite/`에 웹 스크래핑(DuckDuckGo) 기능을 뺀 배포용 변형 완성(`downloader.py`,
  `requirements.txt`, `setup.iss`, `README.md`). Mouser 공식 API만 쓰고, 못 찾으면 참고 링크만 안내.
- 개인 사용 목적으로, 사용자 승인 하에 실제 Mouser API 키를 설치파일(`Setup.exe`)에 직접 번들링해서
  `%AppData%\DatasheetDownloader\.env`에 바로 까는 옵션을 `setup.iss`에 추가함(`onlyifdoesntexist`
  플래그로 기존 키는 안 덮어씀). 이 방식으로 빌드한 결과물이
  `C:\Users\Root\Desktop\클로드\00. 배포용\데이터시트다운로더Lite_설치.exe`에 있음.

### 미해결 — 다음에 이어받을 때 최우선으로 볼 것
사용자가 실제로 이 설치파일을 돌려서 다운로드를 시도했더니 22개 품번 전부
`Mouser API 오류: Invalid unique identifier.`로 실패함. 조사한 결과:

1. **API 키/요청 코드 자체는 문제없음** — 배포된 `.env`의 키를 직접 꺼내서 Mouser API에 라이브로
   테스트했고, 정상 응답이 옴(키 해시로 원본과 동일함도 확인).
2. **훨씬 심각한 별도 문제 발견**: 설치된 프로그램 폴더(`%LocalAppData%\DatasheetDownloaderLite`)를
   열어보니, 최초 셋업 때 설치했던 Python 3.14 자체가 통째로 사라져 있었음(`.venv\Scripts\`에
   `pythonw.exe`만 남고 `python.exe`/`pip`/`pyvenv.cfg`가 없어짐, 기반 설치 폴더
   `%LocalAppData%\Programs\Python\Python314`도 사라짐). 사용자가 마지막으로 정상 실행한 로그
   (22개 품번을 순회하며 실제 네트워크 요청까지 한 기록)와 이 확인 사이 짧은 시간 안에 벌어진
   일이라, **프로그램이 스스로 지운 게 아니라 외부 요인(Windows Defender로 추정 — 이 PC엔 Defender만
   실행 중, 실시간 보호 켜져 있음 확인)이 지웠을 가능성이 높음.** 서명 안 된 python.exe가 AppData
   밑에 조용히 설치되고 곧바로 외부 네트워크 요청을 하는 패턴은 Defender 휴리스틱 오탐의 흔한
   케이스.
3. **가설**: 같은 Defender의 실시간 보호(웹/네트워크 검사)가 파일 격리뿐 아니라 Mouser API로 나가는
   HTTPS 요청 자체(특히 URL의 `apiKey` 쿼리 파라미터)를 검사하며 손상시켰을 가능성 — 이게 맞다면
   "Invalid unique identifier" 오류의 진짜 원인. 아직 확정은 못 함(사용자 확인 대기 중).

**다음에 확인할 것**: 사용자에게 ① Windows 보안 → 바이러스 및 위협 방지 → 보호 기록에서 관련 항목
복원, ② `%LocalAppData%\DatasheetDownloaderLite` 폴더를 Defender 제외 항목에 추가, ③ 재실행(Python
자동 재설치됨) 후 재시도, ④ 그래도 안 되면 Defender 실시간 보호를 잠깐 꺼보고 테스트해달라고
요청해둔 상태 — 결과를 아직 못 받음. `launch.ps1`은 `.venv\Scripts\python.exe`가 없으면 자동으로
처음 셋업을 다시 돈다는 것도 확인함(자가치유는 되지만, 같은 원인이 반복되면 매번 재발할 수 있음).
