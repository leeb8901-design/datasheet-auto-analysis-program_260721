# 작업 인계 문서 (Handoff) — 2026-07-31

> 다른 환경에서 이어서 작업하기 위한 인계용. (이전 문서: `HANDOFF_2026-07-30.md`)
> 프로젝트: 데이터시트 자동분석 → PTC Windchill(MIL-HDBK-217F Notice 2) 신뢰도 파라미터 생성.

---

## 0. 가장 큰 변화 — '작업지' 중심으로 통합

- **'작업지' = 루트의 `Data_list_217F.xlsx`** (이전 `Data_list_217F_수정.xlsx`에서 이름 변경). 사용자가 "작업지"라고 하면 이 파일을 뜻함.
- 이 한 파일에 **입력 + 모든 산출물**을 담는 구조로 통합했음. 별도 출력지(매핑맵 워크북)와 PDF 주석 기능은 **폐지**.
- `tools/build_reference.py`의 기본 입력도 `Data_list_217F.xlsx`로 변경됨.

### 작업지 구조 (2개 시트를 앱이 채움)

**시트 `부품리스트`** (헤더가 **2행**에 있음 — 리더/라이터가 헤더행 자동감지):
| B | C | D | E | F | G | H |
|---|---|---|---|---|---|---|
| 순번 | 품명 | 품번 | 제조사 | 상태 | 데이터시트 다운로드 링크 | 분석된 데이터시트 링크 |
| ← 사용자 입력 → | | | | ← 앱이 분석 후 기록 → | | |

- 앱 기록 매핑: 다운로드 상태 → `상태`, 데이터시트 링크 → `데이터시트 다운로드 링크`, 저장 경로 → `분석된 데이터시트 링크`. (`utils/config.py`의 `WORKSHEET_RESULT_COLUMN_MAP`)
- `분석 상태`·`오류내용`·`미확인 항목`·`대분류` 열은 **삭제**됨(더 이상 안 씀).

**시트 `PSA 입력 파라미터`** (파라미터 값 기록 — 신규 핵심 기능, `excel/psa_writer.py`):
- 각 소분류 '정의행'(SD25~ 슬롯에 파라미터 **이름**) 아래에 **부품마다 한 행**을 추가(PartNumber·품명 + SD열 정렬).
- **정의행 셀 색이 범례의 "관련자료(Datasheet)·분석결과값 적용" 스와치 색(= L5 셀, theme 4/tint 0.6)과 같은 파라미터에만** 분석값 기입.
- 그 색인데 앱이 확인 못하면 **공백 + 노란색(FFFFFF00)**. 다른 색(Relex 자동계산=theme7 / 설계자=theme9 / Default=무색)은 **앱이 안 건드림**.
- **기존 4색 규칙(흰/회/보라/분홍)은 폐지.** (단 `excel/mapping_colors.py`·`mapping_template_builder.py`는 `diagnostics/self_check.py`가 아직 참조하므로 파일은 남겨둠.)

---

## 1. 오늘 변경한 파일

| 파일 | 변경 |
|---|---|
| `tools/build_reference.py` | 신규. 엑셀→`data/*.json` 생성. **`headers.json`도 함께 생성**(subcat_params 파라미터 union → 어휘 불일치 구조적 방지). 기본 입력 `Data_list_217F.xlsx`. |
| `ai/reference.py` | 신규. 품질등급/θJC·θJA/MTBF 판정. `resolve_thermal_resistance(mode="case"/"ambient")`. `parse_temp_range` 강건화(범위+온도단위 앵커, 동작>저장 우선). |
| `ai/pdf_parser.py` | `analyze_pdf(thermal_mode)`; 참고표 자동판정(Quality Level·Thermal Resistance) 연결; 동작온도를 원문에서 파싱. `reference_notes` 반환. |
| `ai/analysis_state.py` | `PartAnalysis`에 `reference_notes` + `unresolved_summary()`. |
| `ai/classifier.py` | IC 소분류 `Custom` 제거(97개 정본에 없어서). |
| `ai/prompt.py` | 주석/개수 최신화(새 입력지 기준). |
| `excel/excel_reader.py` | **헤더행 자동감지**(`find_header_row`), 품명(part_name) 읽기. |
| `excel/excel_writer.py` | 헤더행 자동감지 + `WORKSHEET_RESULT_COLUMN_MAP`으로 상태/링크 기록. **`write_part_params()`** 추가(같은 저장에서 PSA 시트도 기록). |
| `excel/psa_writer.py` | 신규. PSA 시트에 부품 행 추가 + 데이터시트색 파라미터 기록. |
| `ui/main_window.py` | 열저항 Case/Ambient 콤보 추가 → 분석에 전달. 워커가 PSA 기록 호출. **'출력' 버튼·주석 기능 제거.** Import 양식 버튼 → `Data_list_217F.xlsx` 내보냄. |
| `ui/analysis_dialog.py` | 검토창에 자동판정 근거 툴팁. |
| `datasheet/downloader.py` | **`real_chrome=True`**(스크래핑에 시스템 Chrome 사용). |
| `utils/config.py` | `WORKSHEET_RESULT_COLUMN_MAP`, `PART_NAME_KEYWORDS`, IMPORT_TEMPLATE_PATH→작업지. |
| `초기설정.ps1` | patchright 브라우저 설치 + 실제 Chrome 점검 단계 추가(아래 3번). |
| (삭제) `excel/output_builder.py`, `datasheet/annotator.py` | 별도 출력지/주석 폐지. |
| `data/*.json` | build_reference.py 재생성 결과(headers 93컬럼, subcat_params 97개 등). |

---

## 2. 확정된 규칙/결정

- **subcat_params 정본 = 새 입력지 PSA 97개** (옛 154개 폐기). headers.json도 같은 엑셀에서 생성해 어휘 일치.
- 엑셀 PSA 시트 오타 7종 수정(Operating Power/Initial Temp Rise/Metallization/Utilization/Chips-Package/Joint Height/Max Joining Distance).
- **열저항 기준**은 GUI에서 Case(θJC)/Ambient(θJA) 선택. 기본 Case(θJC 표 폴백). Ambient는 데이터시트값만.
- **온도 기반 Quality Level** 자동판정이 실제 작동(원문에서 동작온도 파싱). `parse_temp_range` 오검출(품번 숫자) 수정.

---

## 3. 다른 환경에서 셋업 (중요 — 오늘 실제로 겪은 함정 포함)

1. `초기설정.ps1` 실행. 하는 일: Python 3.14 확인 → `.venv` → pip 설치 → **playwright + patchright 브라우저 설치** → **실제 Chrome 점검** → `.env` 안내.
2. **`.env` 직접 준비**: `MOUSER_API_KEY=...` (git 미포함).
3. **다운로드 브라우저**: scrapling `StealthyFetcher`가 내려받는 Chrome-for-Testing이 일부 Windows에서 side-by-side 오류(`spawn UNKNOWN` / `Executable doesn't exist`)로 실행이 안 됨. 그래서 코드가 **시스템에 설치된 실제 Google Chrome**을 사용함(`real_chrome=True`). → **실제 Chrome 설치 필요**(`winget install --id Google.Chrome -e`). 없으면 `SCRAPLING_REAL_CHROME=0`로 번들 브라우저 강제.
4. 데이터 재생성이 필요하면(엑셀 수정 후): `python tools/build_reference.py` → `data/*.json` 갱신.
5. 실행: `프로그램_실행.bat`.

---

## 4. 남은 작업 (TODO)

1. **실제 제조사 데이터시트로 end-to-end 검증** — PSA 파라미터 기록 정확도(패키지/온도/값)까지. 오늘은 합성 PDF로만 검증함.
2. **작업지의 현재 8개 부품**에 대해 실제 분석 실행 → 부품리스트 F/G/H + PSA 시트 채워지는지 실물 확인(GUI). (지금 부품리스트엔 이전 결과가 있고, PSA 시트는 아직 부품 행 없음.)
3. **4색 규칙 잔재 정리** — CLAUDE.md의 옛 색 규칙/매핑맵 설명은 이제 폐지된 방식. `mapping_*`·`self_check`가 옛 규칙을 참조 중이라, 정본화하려면 self_check도 새 PSA 방식으로 정리 필요.
4. **PSA 재실행 시 중복 행** — 같은 부품을 다시 분석하면 PSA에 부품 행이 또 추가됨(현재 dedupe 없음). 필요하면 품번 기준 갱신 로직 추가.
5. θJC/θJA 선택값이 Temperature Rise 계산까지 일관되게 반영되는지(HANDOFF 2026-07-30 §4.4 연장선) 재점검.

---

## 5. 주의사항

- **LibreOffice/recalc 재저장 금지** (색상 소실 사고 이력). openpyxl로 값/서식만 쓰고 그대로 저장.
- 작업지 수정 스크립트는 항상 **백업 후** 진행(오늘은 스크래치패드에 백업 보관).
- `.env`(MOUSER_API_KEY)는 절대 커밋 금지(.gitignore 등록됨).
