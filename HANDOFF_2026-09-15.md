# 작업 인계 문서 (Handoff) — 2026-09-15

> 다른 환경에서 이어서 작업하기 위한 인계용. (이전 문서: `HANDOFF_2026-09-09.md`)
> 프로젝트: 데이터시트 자동분석 → PTC Windchill(MIL-HDBK-217F Notice 2) 신뢰도 파라미터 생성.
>
> 이번 문서는 기능 개발이 아니라 **보안/저작권 감사와 문서 정합성 점검**을 다룬다. 상세 근거는
> [`보안점검_2026-09-11.md`](보안점검_2026-09-11.md)(전체 조사 기록), 요약 대시보드는
> [`보안_해킹_점검.md`](보안_해킹_점검.md)/[`저작권_라이선스_점검.md`](저작권_라이선스_점검.md) 참고.

---

## 0. 가장 큰 변화

이번 세션(2026-09-11 ~ 09-15)은 크게 세 갈래로 진행됨:

1. **문서-코드 불일치 정리** — `소프트웨어_설계문서.md`/`old_downloader.md`가 이미 폐지된 옛
   4색 매핑맵 방식을 여전히 설명하고 있던 걸 실제 코드(PSA 방식)에 맞게 재작성, 죽은 코드
   `excel/mapping_writer.py` 삭제.
2. **패키징 보안 감사** — Setup.exe로 배포했을 때 인터넷에서 뭘 받는지, 원격 접속/백도어
   가능성이 있는지 전수 조사.
3. **저작권 침해 요소 8개 발견 및 처리** — 그중 가장 큰 건이 `pymupdf`(AGPL) 제거였음.

**✅ 이번 세션의 모든 변경사항은 git에 커밋·푸시 완료됨**(마지막 커밋 `d48de02`). 단, 이번엔
평범한 커밋만 있었던 게 아니라 **`git filter-repo`로 히스토리를 재작성하고 강제 푸시**한 것도
있음(§2 참고) — 다른 환경에서 이어받을 때는 기존 클론을 `git pull`하지 말고, **다시
클론(`git clone`)하거나 최소한 `git fetch origin && git reset --hard origin/master`로
로컬을 원격에 맞출 것.** 히스토리 자체가 바뀌어서 일반 `pull`은 충돌하거나 옛 커밋을 다시
병합해버릴 수 있음.

---

## 1. 문서-코드 불일치 정리 (2026-09-11)

| 항목 | 내용 |
|---|---|
| `소프트웨어_설계문서.md` | §2 폴더구조(`output_builder.py` 등 존재하지 않는 파일 제거), §3.5 출력지 생성, §3.6 PDF 주석, §3.7 self_check, §4 mapping_colors, §5 구현현황표, §5-1 VBA 파일명, §6 알려진이슈를 실제 코드에 맞게 재작성 |
| `old_downloader.md` | PDF 주석 재도입(2026-09-03)/API 키 방식 전환(2026-09-04) 반영 — 이 문서는 2026-08-18 시점 스냅샷이라 이후 변경사항은 "[2026-09 갱신]" 표시로 구분 |
| `excel/mapping_writer.py` | 완전히 죽은 코드(어디서도 import 안 됨) 확인 후 삭제 |

## 2. 패키징 보안 감사 (2026-09-11, 2026-09-13)

**요약 대시보드**: [`보안_해킹_점검.md`](보안_해킹_점검.md) — 여기서는 핵심만.

- 설치 시(1회성) 인터넷에서 받는 것: Python 패키지(pip, 버전 고정 없음), scrapling→Camoufox,
  patchright→Chromium(패치빌드), Google Chrome(winget 실패 시 무음 직접 설치).
- 실행 중 인터넷에 연결되는 지점은 **"다운로드" 버튼 하나뿐** — Mouser API → DuckDuckGo →
  임의 도메인에서 PDF 수신. 분석/저장/엑셀 조작 등 나머지 전부는 로컬 처리(코드 전체 grep으로
  확인).
- **원격 연결/백도어 가능성: 없음** — 서버 소켓, 원격제어 도구, 텔레메트리, 시작프로그램
  등록, 예약작업, 방화벽 규칙 변경, 외부 개방 디버그 포트 전부 grep/직접 확인, 전부 해당 없음.
- 남은 권고: pip 버전 고정 + 해시 검증 도입, Chrome 무음 설치 투명성 개선(둘 다 미착수).

## 3. 저작권 침해 요소 8개 처리 (2026-09-13 ~ 15)

**요약 대시보드**: [`저작권_라이선스_점검.md`](저작권_라이선스_점검.md) — 8개 항목 전부 해결/종결됨.

가장 중요한 두 가지만 여기 남긴다:

### 3-1. 예시 데이터시트 PDF 4종 — git 히스토리 완전 제거 (2026-09-15)

`IS31FL3296-UTLS4-TR_예시.pdf`/`_예시안.pdf`, `클로드 학습자료/IS31FL3296-UTLS4-TR_클로드분석.pdf`/
`SI53340-B-GM_클로드분석.pdf` 4개가 이미 공개 GitHub 저장소(origin/master)에 푸시된 상태였음을
발견 → `git-filter-repo --invert-paths`로 42개 커밋 전체에서 제거 후 강제 푸시.

**⚠️ 놓치기 쉬운 함정**: 원격에 `lite-v1.1.0`이라는 **태그**가 있었는데, 이 태그가 가리키던
옛 커밋(PDF 도입 커밋의 후손)에도 PDF가 그대로 남아있었음. `master`만 강제 푸시하면 이 태그를
통해 계속 접근 가능했을 것 — **브랜치뿐 아니라 태그까지 `git ls-remote origin`으로 전수 확인
후 히스토리를 재작성할 것.** 태그도 새 히스토리의 대응 커밋으로 옮겨 강제 푸시함.

부품리스트 품번만 담은 xlsx 4종(`Data_list_217F_예시.xlsx` 등)은 PDF 원본 내용이 없어(품번·
경로 텍스트뿐) 저작권 대상이 아니라고 판단, 그대로 유지함 — 이번에 다시 확인해보니 정상적으로
커밋·푸시된 상태 그대로임.

### 3-2. `pymupdf`(AGPL-3.0/Artifex 상용 듀얼) 제거 (2026-09-15)

`datasheet/annotator.py`의 PDF 하이라이트+메모 삽입 기능이 pymupdf(`fitz`)에 의존하고 있었음.
`pypdfium2`/`pdfminer.six`로 바로 교체하려 했으나, **이 둘은 순수 읽기 전용이라 PDF에 주석을
쓰는 기능 자체가 없음**(실제 API 조사로 확인, `pypdfium2.PdfPage`에 annotation 관련 메서드
0개). 그래서 `pypdf`(BSD-3-Clause)를 추가로 도입:
- 문구 위치 찾기: `pdfplumber.Page.search()` (기존에도 쓰던 라이브러리)
- 하이라이트+메모 작성: `pypdf.annotations.Highlight`

**좌표계 함정**: pdfplumber는 왼쪽위 원점(y 아래로 증가), PDF 네이티브 좌표(pypdf의 `/Rect`,
`/QuadPoints`)는 왼쪽아래 원점(y 위로 증가) — `datasheet/annotator.py`의 `_to_pdf_rect()`가
`page_height - y`로 뒤집는다. 개발 중 `_quad_points()`에서 좌표를 `FloatObject`로 안 감싸서
`'float' object has no attribute 'write_to_stream'` 오류가 났던 걸 실제로 겪고 고쳤음 — 이
클래스의 좌표는 반드시 `FloatObject`로 감싸야 함.

실제 로컬 데이터시트(`Download_ datasheets/Integrated Circuit/Linear/IS31FL3296-UTLS4-TR.pdf`)로
`analyze_pdf()`→`annotate_pdf()` 전 과정 검증: 4개 필드 하이라이트 좌표를 pdfplumber bbox와
역산 대조해 정확히 일치함을 확인, 2회 연속 실행해도 주석이 4개로 유지됨(중복 안 쌓임, 기존
멱등성 로직을 `/Annots` 배열 직접 순회 방식으로 재구현). `.venv`에서 `pymupdf`를 완전히
삭제한 뒤에도 앱 핵심 모듈 전부 정상 import 확인.

`requirements.txt`/`installer/lite/requirements.txt`에서 `pymupdf` 제거, `pypdf` 추가.
`THIRD-PARTY-NOTICES.md`(40개 패키지 라이선스 고지문, 2026-09-15 최초 생성 후 이 교체로 재생성)
재생성 완료.

---

## 4. 다른 환경에서 셋업 시 주의

`HANDOFF_2026-09-09.md` §3과 대부분 동일. 추가된 부분만:

1. **이 저장소는 git 히스토리가 재작성됨(2026-09-15)** — 기존에 클론해둔 사본이 있다면
   `git pull` 대신 새로 클론하거나 `git fetch origin && git reset --hard origin/master`로
   맞출 것. 이전 클론에는 이미 제거된 PDF 4종이 로컬에 남아있을 수 있음(git 특성상 원격
   히스토리 재작성만으로는 이미 받아간 사본까지 지울 수 없음 — 필요하면 수동 삭제).
2. `requirements.txt`가 바뀌었으니(`pymupdf` 제거, `pypdf` 추가) 기존 `.venv`가 있다면
   `pip install -r requirements.txt`를 다시 돌려 동기화할 것(`pip uninstall pymupdf`도
   같이 해주는 게 깔끔함 — 안 지워도 동작엔 지장 없지만 불필요한 AGPL 패키지가 로컬에 남음).

## 5. 남은 작업 (TODO)

1. **pip 패키지 버전 고정 + 해시 검증** — `requirements.txt`에 버전이 전혀 고정 안 되어 있어
   공급망 공격에 취약. 미착수.
2. **임의 도메인(애그리게이터) PDF 다운로드 안전장치** — 화이트리스트/차단리스트 없음. 사용자가
   "현상 유지"로 결정(2026-09-15), 재검토 시 참고.
3. **Chrome 무음 자동 설치 투명성** — 별도 동의 단계 분리 미착수.
4. **`tools/annotate_datasheet.py`/`tools/annotate_si53340.py` 정리** — 아직 `import fitz`가
   남아있으나 대상 파일이 이미 삭제돼(§3-1) 실행 자체가 안 되는 죽은 스크립트. 삭제 여부 미결정.
5. **Mouser API 키 보안(저장/전송 방식)** — 이번 감사에서 사용자가 명시적으로 제외한 다음 라운드
   후보.

## 6. 더 보기

- [`보안점검_2026-09-11.md`](보안점검_2026-09-11.md) — 이번 감사의 전체 조사 과정과 근거(가장 상세함).
- [`보안_해킹_점검.md`](보안_해킹_점검.md) — 해킹/네트워크 노출 현황 대시보드(관리용).
- [`저작권_라이선스_점검.md`](저작권_라이선스_점검.md) — 저작권/라이선스 현황 대시보드(관리용).
- [`THIRD-PARTY-NOTICES.md`](THIRD-PARTY-NOTICES.md) — 오픈소스 라이선스 고지문(패키지 40종).
- [`CLAUDE.md`](CLAUDE.md) §5-1, [`소프트웨어_설계문서.md`](소프트웨어_설계문서.md) §3.6 — PDF 주석 기능의 최신 구현 설명.
