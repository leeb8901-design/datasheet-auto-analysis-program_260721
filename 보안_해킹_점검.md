# 보안(해킹 노출) 점검 현황

> **이 문서의 목적**: 이 프로그램을 패키징해 다른 PC에 배포했을 때 "나도 모르는 불법 프로그램
> 설치, 해킹 노출, 원격 접속 가능성"을 한눈에 보고 관리하기 위한 대시보드. 새로 점검할 게
> 생기면 아래 표에 추가할 것. 상세 조사 방법/근거는 [`보안점검_2026-09-11.md`](보안점검_2026-09-11.md)와
> [`HANDOFF_2026-09-15.md`](HANDOFF_2026-09-15.md)에 있음 — 저작권/라이선스 쪽은
> [`저작권_라이선스_점검.md`](저작권_라이선스_점검.md)로 분리했음.
>
> 마우저 API 키 자체의 보안(저장/전송 방식)은 이 문서 범위가 아님 — 별도 라운드 예정.

---

## 1. 인터넷 연결 지점 (2026-09-11 전수 조사)

### 설치 시(최초 1회, `installer/setup_env.ps1`)

| 받는 것 | 출처 | 위험도 | 비고 |
|---|---|---|---|
| Python 3.14.6 설치 프로그램 | Setup.exe 내부 번들 | 🟢 낮음 | 설치 시점엔 인터넷 불필요 |
| pip 패키지(현재 7종 직접 + 33종 하위) | pypi.org | 🟡 중간 | **버전 고정 없음** — 미해결(§3 TODO) |
| 브라우저 자동화 바이너리 #1 | scrapling → Camoufox(GitHub Releases) | 🟡 중간 | 해시/서명 검증 없이 실행 |
| 브라우저 자동화 바이너리 #2 | patchright → Chromium(패치 빌드) | 🟡 중간 | 자동화 탐지 우회 특화 빌드 |
| Google Chrome 설치 프로그램 | winget 실패 시 dl.google.com에서 무음 설치 | 🟡 중간 | 사용자 별도 동의 없이 시스템에 설치될 수 있음 — 미해결(§3 TODO) |

### 실행 중 (기능별)

| 기능(트리거) | 연결 여부 | 내용 |
|---|---|---|
| **다운로드 시작** 버튼 | ✅ 연결됨 | Mouser API 검색 → DuckDuckGo 웹검색 → 검색 결과의 임의 도메인에서 PDF 수신 |
| 신뢰도 분석 / 저장 / 열기 등 나머지 전부 | ❌ 연결 안 됨 | 로컬 파일·PDF 처리만 함(코드 전체 grep으로 확인 — `ai/`, `excel/` 네트워크 호출 0건) |
| 엑셀 매크로 `datasheet_helper.bas` | ✅ 수동 실행 시만 | 다운로드 실패 행의 참고링크를 WinHttp로 직접 수신(자동 아님) |
| `diagnostics/self_check.py`의 Mouser 연결 테스트 | 코드만 존재, 미실행 | GUI 어디에도 연결 안 됨 |

---

## 2. 원격 연결 / 백도어 가능성 점검 (2026-09-11)

코드 전체(`.venv` 제외)를 아래 패턴으로 grep해 확인. **전부 해당 없음.**

| 확인 항목 | 결과 |
|---|---|
| 서버 소켓/웹서버(bind/listen, Flask, FastAPI, uvicorn, http.server 등) | 없음 |
| 원격 제어 도구 흔적(VNC/TeamViewer/역방향 쉘) | 없음 |
| 텔레메트리/사용현황 전송(beacon, analytics, phone-home) | 없음 |
| Windows 시작프로그램 등록(레지스트리 Run 키) | 없음 |
| 예약 작업(Task Scheduler) 등록 | 없음 |
| 방화벽 규칙 변경(netsh/advfirewall) | 없음 |
| 브라우저 자동화(CDP) 디버그 포트를 외부(0.0.0.0)로 개방 | 없음 — 기본값(루프백 전용) |

**결론**: 나가는 연결(outbound)만 있고 들어오는 연결(inbound/서버 역할)은 코드상 존재하지 않음.
지속성(persistence) 메커니즘도 없음.

---

## 3. 남은 권고 (미해결)

| # | 항목 | 위험도 | 상태 |
|---|---|---|---|
| 1 | pip 패키지 버전 고정 + 해시 검증(`--require-hashes`) 부재 | 🟡 중간 | 미착수 |
| 2 | 검색으로 찾은 임의 도메인 PDF — 화이트리스트/차단리스트 없음, 검증은 `%PDF` 매직바이트뿐 | 🟡 중간 | 사용자가 "현상 유지"로 확정(2026-09-15) — 저작권_라이선스_점검.md 5번과 동일 사안 |
| 3 | Chrome 무음 자동 설치를 별도 동의 단계로 분리 | 🟡 중간 | 미착수 |
| 4 | Python/Chrome 설치기·pip 패키지 다운로드물 무결성(해시/서명) 검증 부재 | 🟡 중간 | 미착수(1번과 연관) |

## 4. 이번 점검에서 제외한 것 (다음 라운드 후보)

- Mouser API 키 저장/전송 방식 보안(`User_API/` 폴더, 평문 저장 여부 등).
- `.bas` 매크로(`vba/datasheet_helper.bas`) 자체의 코드 인젝션/매크로 보안 심층 검토.
- 다운로드된 PDF를 여는 뷰어(`pypdf`/외부 프로그램) 자체의 알려진 CVE 여부.

## 참고 문서

- [`보안점검_2026-09-11.md`](보안점검_2026-09-11.md) — 전체 조사 과정(가장 상세함).
- [`저작권_라이선스_점검.md`](저작권_라이선스_점검.md) — 저작권/오픈소스 라이선스 현황(분리 관리).
- [`HANDOFF_2026-09-15.md`](HANDOFF_2026-09-15.md) — 이번 라운드 작업 요약.
