# 작업 인계 문서 (Handoff) — 2026-09-09

> 다른 환경에서 이어서 작업하기 위한 인계용. (이전 문서: `HANDOFF_2026-09-04.md`)
> 프로젝트: 데이터시트 자동분석 → PTC Windchill(MIL-HDBK-217F Notice 2) 신뢰도 파라미터 생성.

---

## 0. 가장 큰 변화

이번 세션(2026-09-05 ~ 09-09)은 크게 다섯 갈래로 진행됨:

1. **API 키 관리 방식을 통째로 교체** — `.env` 파일 방식을 폐지하고, `User_API/` 폴더(API 하나당
   파일 하나)로 바꿈. 프로그램 안에 "API 키 관리" 창도 새로 생김.
2. **Mouser 다운로드 관련 버그 수정 + 성공률 개선** — HTTP 202(Accepted) 비동기 응답 처리, 검색어
   정규화 재시도, ProductDetailUrl 폴백, Rate Limit 방지 딜레이 등 4가지.
3. **GUI 안전장치 2가지** — PDF가 다른 프로그램에 열려 있으면 "신뢰도 분석" 차단, "출력지 저장"
   기본 위치를 "입력지 양식"과 통일.
4. **시도했다가 되돌린 것** — Mouser 재시도 로직 + 콘솔(cmd) 진행창은 한 번 만들었다가 사용자
   요청으로 전부 되돌림(git에 커밋 안 된 상태였어서 `git restore`로 깔끔하게 원복됨).
5. **배포판(Lite) 재동기화 + 재빌드** — 위 변경사항들이 반영 안 된 채로 멈춰있던 `installer/lite/`
   전체를 최신 원본과 다시 맞추고, 실제로 설치파일까지 다시 뽑아 검증함.

**⚠️ 중요: 이 세션의 모든 변경사항은 아직 git 커밋이 안 된 상태입니다** (`git status`로 확인
가능). 다른 환경에서 이어받으려면 이 저장소를 그대로 복사하거나, 커밋부터 먼저 하고 푸시해야
합니다 - 커밋 여부는 항상 사용자가 명시적으로 요청할 때만 진행하는 규칙이라 아직 안 했습니다.

---

## 1. 이번 세션에 변경한 파일

| 파일 | 변경 |
|---|---|
| `utils/config.py` | `.env`/`python-dotenv` 관련 코드 전부 제거(`USER_CONFIG_DIR`/`USER_ENV_PATH`/`LEGACY_ENV_PATH`/`ENV_SEARCH_PATHS`/`load_dotenv`/마이그레이션). 대신 `USER_API_DIR = APP_DIR / "User_API"`, `read_user_api_files()`(폴더 안 모든 파일을 매번 새로 읽어 `KEY=VALUE` 파싱), `get_api_key(name)`/`get_mouser_api_key()` 추가. |
| `datasheet/search.py` | `MouserClient.__init__`이 캐시된 상수 대신 `get_mouser_api_key()`를 매번 호출(재시작 없이 즉시 반영). **검색어 정규화 재시도**: 원본 품번으로 정확히 일치하는 결과가 없으면 특수문자를 뺀 순수 영숫자로 한 번 더 검색(`_normalize_part_number`, `_search_once`, `_pick_exact_match`). `search_part()` 반환값에 `product_detail_url` 필드 추가. |
| `datasheet/downloader.py` | **HTTP 202 폴링 신설**(`_header_get`/`_parse_retry_after`/`_poll_async_202`) — `_download_once()`와 `_fetch_ddg_html()` 둘 다 202를 받으면 `Location`/`Retry-After`로 최대 10회 재확인. **Mouser 전용 가벼운 다운로드**(`MOUSER_PDF_HEADERS`, `_fetch_pdf_direct`, `_fetch_datasheet_url_from_product_page`, `_download_via_mouser_url`) — requests로 먼저 가볍게 시도, 실패하면 기존 브라우저 방식(`download_pdf`)으로 자동 폴백. **Rate Limit 딜레이**(`RATE_LIMIT_DELAY_RANGE`, Mouser API 호출 직전 0.5~1.0초 대기). `is_pdf_locked(path)` 신설(PDF가 다른 프로그램에 열려 있는지 확인). |
| `ui/api_manager_dialog.py` | **신규 파일**. `ApiManagerDialog` — `User_API/` 폴더의 파일 목록을 보여주고, 선택한 파일 내용을 보거나 고치고, 새 파일 추가/삭제도 가능. |
| `ui/main_window.py` | "API 키 관리" 버튼 추가(`_open_api_manager`). `_pdf_path_for_row()` 순서 변경 — 캐시된 경로보다 실제 디스크 상태(`resolve_existing_pdf`)를 항상 먼저 확인. `_locked_pdf_rows()`/`is_pdf_locked` 연동 — 분석 대상 PDF가 열려 있으면 "신뢰도 분석" 시작 자체를 막고 경고. `_save_output()`의 기본 저장 위치를 `EXPORT_DEFAULT_DIR`(고정 폴더) 대신 파일명만 넘기는 방식으로 변경(= "입력지 양식" 버튼과 동일하게 Qt가 마지막 폴더를 기억). |
| `diagnostics/self_check.py` | `MOUSER_API_KEY`/`ENV_SEARCH_PATHS` 대신 `get_mouser_api_key()`/`USER_API_DIR` 사용하도록 갱신. |
| `requirements.txt` | `python-dotenv` 제거. |
| `installer/launch.ps1`, `installer/set_api_key.ps1`, `installer/setup.iss` (원본) | API 키 확인/생성 위치를 `%AppData%\...\.env`에서 `{app}\User_API\`(폴더 안에 파일이 하나라도 있으면 있는 걸로 간주)로 교체. |
| `installer/lite/downloader.py`, `requirements.txt`, `setup.iss`, `README.md` | **전체 재동기화**(2026-09-04 스냅샷에서 멈춰 있던 걸 최신 원본과 다시 맞춤) — 202 폴링/Mouser 가벼운 다운로드/Rate Limit/PDF 잠금 확인 등 DDG 아닌 공통 로직 전부 반영. `.env` 번들 대신 `user_api_bundle\User_API\*` → `{app}\User_API\` 번들링으로 교체. `python-dotenv` 제거, `beautifulsoup4`는 ProductDetailUrl 폴백에 계속 쓰여서 유지. `AppVersion` 1.0.0 → 1.1.0. |
| `test_Import_User.xlsx` (신규, 아직 git 미추적) | 사용자가 만든 테스트용 입력지 예시 파일. 가장 최근 Lite 재패키징(임시로 `00. 배포용\222`에 출력)에 프로그램 폴더 최상위 파일로 포함시킴 - **이번 한 번만 적용, `setup.iss`의 표준 출력 위치(`00. 배포용`)는 안 건드림**. |
| `CLAUDE.md` | 이번 세션의 모든 결정을 §7 결정 로그에 날짜순으로 기록함(아래 2번 참고). §8 "반드시 지킬 것"의 API 키 관련 항목 갱신. |

**삭제(git상 D로 표시, 실제로는 사용자가 직접 지움)**: 루트의 `111111111111111111111111111111.xlsx`, `2222222222222222222222.xlsx`(예전 테스트용 더미 파일 — `test_Import_User.xlsx`로 대체된 것으로 보임).

**시도했다가 완전히 되돌린 것(git에 흔적 없음)**: Mouser 검색 재시도 로직(`datasheet/search.py`에 3회 재시도+지수백오프로 넣었다가), 콘솔(cmd) 진행창(`installer/progress.ps1`을 GUI 팝업 대신 텍스트+로딩바 콘솔로 바꿨다가) — 둘 다 사용자가 "코드에서 삭제해줘"라고 요청해서 `git restore`로 커밋 이전 상태로 완전히 원복함. **`installer/progress.ps1`은 지금 그 원래(GUI 팝업) 상태 그대로입니다** — 나중에 이 파일이 또 바뀌어 보여도, 콘솔 버전으로 되돌리려던 시도가 아니라 그냥 원상태라는 뜻입니다.

---

## 2. 확정된 규칙/결정 (요약 — 전체 근거는 `CLAUDE.md` §7 결정 로그, 2026-09-04 이후 항목)

- **API 키는 `User_API/` 폴더 방식**(`.env` 폐지, 2026-09-04 결정 → 09-05/09-09 코드/설치파일
  전체 반영 완료). 파일 하나 = API 하나. 파일 "내용"의 `KEY=VALUE`로 무슨 API인지 판단하고,
  파일 "이름"은 사람이 보기 위한 이름표일 뿐(예: `JY_MOUSER_API_KEY.txt` 안에
  `MOUSER_API_KEY=...`). "API 키 관리" 창으로 확인/수정.
- **Mouser 다운로드는 항상 "가벼운 requests 시도 → 실패하면 브라우저(StealthyFetcher) 폴백"
  구조**(2026-09-05 결정). 단, **실측 결과 Mouser 자신의 PDF 호스팅(`mouser.com/datasheet/...`)과
  제품 상세페이지 둘 다 Akamai급 봇 차단이 걸려 있어서, 가벼운 시도는 지금 사실상 항상 실패하고
  브라우저 폴백이 실제로 성공을 만들어냄** — 헤더(User-Agent/Referer)를 아무리 잘 넣어도 이
  차단은 못 뚫음. 코드는 안전하게 폴백하니 문제는 없지만, "가벼운 시도"가 지금 당장 속도上
  이득을 주지는 않는다는 점은 알아둘 것.
- **HTTP 202 응답은 `Location`+`Retry-After`로 최대 10회 폴링**(2026-09-05 결정, `_poll_async_202`).
  `Location` 헤더가 없으면 폴링 자체가 불가능하므로 그 자리에서 명확한 오류로 실패 처리.
- **DuckDuckGo는 이 개발 환경 IP에서 사실상 막혀 있음**(오래된 이슈, 이번 세션에 재확인) — 202를
  주면서 `Location` 헤더도 안 주는 패턴이라(진짜 비동기 작업이 아니라 봇 차단으로 추정), 폴링으로도
  못 뚫음. 기존 "DDG 1회 실패 시 10분 쿨다운"(`_DDG_FAILURE_THRESHOLD=1`) 로직 때문에, 배치 중
  DDG가 한 번이라도 막히면 나머지 전부가 웹검색 단계를 건너뛰고 "찾지 못함"으로 끝남. **이게
  Mouser에 없는 품번들의 실제 최종 실패 원인**(오늘 바꾼 4가지와 무관, 그 이전부터 있던 문제).
  → 배포판(Lite)은 애초에 DDG를 안 써서 이 문제 자체가 없음.
- **PDF가 다른 프로그램(Adobe 등)에 열려 있으면 "신뢰도 분석"을 아예 시작 못 하게 막음**(2026-09-04
  결정, `is_pdf_locked`) — 분석이 PDF를 분류 폴더로 옮기고 주석까지 써야 해서, 열려 있으면 실패하거나
  일부만 반영될 수 있어서 사전 차단. "PDF 열기"는 항상 실제 디스크의 최신 상태를 보여줌.
- **"출력지 저장" 기본 위치는 "입력지 양식"과 항상 같게**(2026-09-05 결정) — 폴더를 고정 지정하지
  않고 파일명만 넘겨서, Qt가 마지막으로 연 폴더를 그대로 따라가게 함.
- **배포판(Lite) 빌드 시 반드시 원본과 재동기화 여부부터 확인할 것**(2026-09-09 결정, 이번에 실제로
  4~5일치 변경사항이 안 반영된 채 방치돼 있던 걸 발견함) — `installer/lite/README.md` 맨 아래에
  두 파일이 공유하는 함수/상수 전체 목록이 있으니, 재빌드 전에 그 목록 기준으로 diff 확인할 것.

---

## 3. 다른 환경에서 셋업

`HANDOFF_2026-09-04.md` §3과 대부분 동일. 추가/변경된 부분만:

1. **API 키 위치가 또 바뀜**: 이제 `.env`가 아니라 프로젝트 루트의 **`User_API/` 폴더** 안에
   텍스트 파일로 둔다(예: `User_API/JY_MOUSER_API_KEY.txt`, 내용 `MOUSER_API_KEY=실제키값`).
   이 폴더는 `.gitignore`에 등록돼 있어서 클론해도 안 따라옴 - 새 환경에서는 이 폴더와 파일을
   직접 만들어야 함. 프로그램 안 "API 키 관리" 버튼으로도 만들 수 있음.
2. 나머지(Python 3.14, 실제 Chrome 필요, `python tools/build_reference.py` 재생성 등)는
   `HANDOFF_2026-07-31.md` §3 / `HANDOFF_2026-09-04.md` §3 그대로.

---

## 4. 남은 작업 (TODO)

1. **DuckDuckGo IP 차단 문제 — 아직 미해결.** 대안(Bing으로 교체, 웹검색 완전 제거하고 참고 링크만
   남기기 등)을 사용자에게 제안해뒀으나 아직 결정 안 됨. 원본(Lite 아닌) 배포판을 계속 쓸 계획이면
   언젠가 다뤄야 함.
2. **`test_Import_User.xlsx`를 표준 빌드 파일 목록에 포함시킬지 결정 안 됨** — 지금은 "이번 한
   번만" 임시로 Lite 패키징에 넣었을 뿐, `installer/빌드방법.md`/`installer/lite/README.md`의
   공식 파일 목록에는 아직 안 넣음. 앞으로도 계속 포함시키고 싶다면 그 문서들도 갱신할 것.
3. **이번 세션 전체가 미커밋 상태** — 커밋/푸시는 사용자가 명시적으로 요청할 때만 진행하는
   규칙이라 아직 안 함. 다음에 이어받을 때 `git status`로 확인하고, 필요하면 커밋부터 요청할 것.
4. `Data_list_217F.xlsx`(작업지)에 최근 개선사항(Mouser 재시도/폴백 등)을 반영해서 실제 22개
   품번으로 재분석한 최신 결과가 반영돼 있는지는 이번 세션에서 다시 확인 안 함 - 필요하면 재검증.

---

## 5. 주의사항

- **LibreOffice 기반 재저장 절대 금지**(`CLAUDE.md` §8).
- API 키(`User_API/` 폴더 전체)는 절대 커밋 금지(`.gitignore`에 등록됨).
- **배포용 설치파일에 실제 API 키를 번들링하는 건 본인만 쓰는 개인 용도에서만** — 사용자가
  명시적으로 승인한 범위. 이렇게 만든 `Setup.exe`는 절대 남에게 공유 금지(내 키가 그대로
  들어있음). 불특정 다수 배포판은 `user_api_bundle` 없이 따로 빌드할 것.
- **Lite 배포판을 다시 빌드하기 전엔 항상 원본과의 동기화 상태부터 확인**(4번 참고 - 이번에
  4~5일치 변경사항이 반영 안 된 채 방치된 걸 실제로 겪음).
- `installer/progress.ps1`은 원래(GUI 팝업 진행창) 상태로 남아있음 - 콘솔 버전 시도는 완전히
  되돌려졌음(0번 참고).

---

## 6. Lite 배포판 현황 (최신, 정상 동작 확인됨)

- **위치**: `C:\Users\Root\Desktop\클로드\00. 배포용\데이터시트다운로더Lite_설치.exe` (표준),
  가장 최근 빌드는 임시로 `00. 배포용\222\`에도 하나 더 있음(`test_Import_User.xlsx` 포함 버전,
  1회성 요청).
- **버전**: 1.1.0 (2026-09-09 재빌드 - 202 폴링/Mouser 성공률 개선/User_API 방식/PDF 잠금 확인
  전부 반영됨).
- **검증 완료**: 스테이징 앱 전체 모듈 import, 실제 부품(T495C107K010ATE100) 라이브 다운로드
  성공, Inno Setup 컴파일 성공, 임시 설치 후 `User_API\JY_MOUSER_API_KEY.txt`가 정확한 자리에
  원본과 동일한 내용으로 깔리는 것·`api_manager_dialog.py` 포함·DDG 코드 없음·202 폴링 코드
  있음까지 확인 후 정리(테스트 설치는 삭제함).
- Mouser API로 실제 22개 품번을 라이브로 돌려서 **성공 10건 / 실패 12건**까지 확인함(실패
  원인은 전부 위 2번의 DDG 차단 - Lite판은 DDG 자체가 없어서 이 실패 사례가 발생 안 함, 원본
  배포판에서만 해당).

---

## 7. 더 보기

- [`CLAUDE.md`](CLAUDE.md) §7 결정 로그의 2026-09-04(뒷부분)~2026-09-09 날짜 항목들 — 이 문서보다
  훨씬 자세한 상황/결정/반영 내용이 각각 적혀있음.
- [`installer/lite/README.md`](installer/lite/README.md) — Lite 재빌드 절차, 원본과 공유하는
  함수/상수 전체 목록(재동기화 체크리스트로 쓸 것).
- [`HANDOFF_2026-09-04.md`](HANDOFF_2026-09-04.md) — 이전 인계 문서.
- [`소프트웨어_설계문서.md`](소프트웨어_설계문서.md) — 프로그램 전체 구현 구조.
