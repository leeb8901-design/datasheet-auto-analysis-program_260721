# 보안 점검 — 2026-09-11

> 목적: 이 프로그램을 패키징(Setup.exe)해서 다른 컴퓨터에 설치·배포할 때, (1) 나도 모르는 불법
> 프로그램/악성코드가 함께 깔리는지, (2) 해킹(외부 침입/원격 제어)에 노출되는 구조가 있는지,
> (3) 저작권/라이선스 문제가 있는지를 점검한 기록. **Mouser API 키 자체의 보안(키 노출/저장 방식)은
> 이번 점검 범위에서 제외**했다(사용자 요청, 별도로 다룰 것).
>
> 관련 문서: [`소프트웨어_설계문서.md`](소프트웨어_설계문서.md)(구조), [`CLAUDE.md`](CLAUDE.md)(분석 규칙).
> 이 문서는 이 둘과 달리 "보안 관점" 전용이라 따로 둔다.

---

## 1. 인터넷 연결 지점 전수 조사

### 1-1. 설치 시(최초 1회, `installer/setup_env.ps1`)

| # | 받는 것 | 출처 | 비고 |
|---|---|---|---|
| 1 | Python 3.14.6 설치 프로그램 | Setup.exe에 이미 번들(빌드 시 python.org에서 미리 받음) — 설치 시점엔 인터넷 불필요 | 이미 Python 있으면 안 씀 |
| 2 | pip 패키지 7종 + 하위 의존성 전체 | PyPI(`pypi.org`/`files.pythonhosted.org`) | `requests`, `openpyxl`, `beautifulsoup4`, `PySide6`, `pdfplumber`, `pymupdf`, `scrapling[fetchers]` — **버전 고정 없음**(`requirements.txt`에 버전 미지정) |
| 3 | 브라우저 자동화 바이너리 #1 | `scrapling.exe install` → Camoufox(변형 Firefox), 주로 GitHub Releases | 무결성 검증(해시/서명) 없이 실행 |
| 4 | 브라우저 자동화 바이너리 #2 | `patchright.exe install chromium` → Playwright 계열 CDN | 자동화 탐지 우회 특화 크로미움 빌드 |
| 5 | Google Chrome 설치 프로그램 | 이미 있으면 스킵. 없으면 ① winget → 실패 시 ② `https://dl.google.com/chrome/install/standalonesetup64.exe`를 해시 검증 없이 받아 `/silent` 설치 | 사용자 별도 동의 없이 시스템에 프로그램 하나가 더 깔릴 수 있음 |

### 1-2. 실행 중(프로그램을 쓰는 동안)

| 기능(트리거) | 인터넷 연결 | 내용 |
|---|---|---|
| **다운로드 시작** 버튼 | ✅ | 행마다 ① Mouser API 검색(`api.mouser.com`, 이번 점검 제외) → ② 실패 시 DuckDuckGo 웹검색(`html.duckduckgo.com`) → ③ **검색 결과에 뜬 임의 도메인**을 실제 브라우저로 열어 PDF 수신 → ④ Mouser 제품페이지/직링크(`mouser.com`, API 아닌 일반 웹) |
| 신뢰도 분석 / API 키 관리 / 입력지·출력지 저장 / PDF·폴더 열기 / 새로고침 | ❌ | 전부 로컬 파일·PDF 처리만 함(`ai/*.py`, `excel/*.py`가 네트워크 호출 전무를 코드 전체 grep으로 확인) |
| `vba/datasheet_helper.bas`(엑셀 매크로) | ✅, 단 **수동** | 자동 다운로드 실패 행에 한해 사용자가 엑셀에서 직접 실행할 때만 WinHttp로 참고링크를 받음 |
| `diagnostics/self_check.py`의 Mouser 연결 테스트 | 코드는 있으나 **미실행** | GUI 어디에도 연결 안 된 개발자용 죽은 코드 |

**결론**: 자동으로 인터넷에 붙는 지점은 사실상 **"다운로드" 버튼 하나**뿐이고, 그 안에서 가장 통제가 안 되는 부분은 "검색 결과에 뜬 임의 도메인에서 PDF를 받는 단계"다.

---

## 2. 우려사항별 위험 매칭

| # | 항목 | 🦠 불법 프로그램/악성코드 위험 | 🔓 해킹 노출 위험 | ⚖️ 저작권 침해 위험 |
|---|---|---|---|---|
| 설치-1 | Python 설치 프로그램(번들) | 낮음 | 중간 — 해시/서명 검증 없이 실행 | 없음 |
| 설치-2 | pip 패키지 7종(PyPI) | 중간 — 버전 고정 없음(공급망 공격 노출) | 중간 — 해시 검증(`--require-hashes`) 없음 | **중간~높음 — `pymupdf`가 AGPL-3.0/Artifex 상용 듀얼 라이선스로 확인됨(pip 메타데이터로 직접 확인). 상용 라이선스 없이 무료 배포 시 법적 검토 필요** |
| 설치-3 | scrapling → Camoufox | 중간 — 검증 없이 실행 | 중간 — 봇탐지 우회 목적 브라우저 | 낮음 |
| 설치-4 | patchright → 크로미움(패치 빌드) | 중간 | 중간~높음 — 자동화 탐지 우회 특화, 대상 사이트 ToS 위반 소지(회색지대) | 낮음 |
| 설치-5 | Chrome 무음 자동 설치 | 낮음(Chrome 자체는 정상) | 중간 — 해시 검증 없는 무음 설치 | 없음 |
| 실행-1 | DuckDuckGo 검색 HTML | 낮음 | 낮음 | 없음 |
| 실행-2 | **검색으로 찾은 임의 도메인의 PDF** | **높음 — 화이트리스트 없음, 검증은 `%PDF` 매직바이트뿐** | **높음 — 신뢰 안 된 사이트를 실브라우저로 방문(드라이브바이 노출면), SEO 포이즈닝 이론상 가능** | 중간 — 애그리게이터 사이트의 무단 재호스팅 가능성 |
| 실행-3 | Mouser 제품페이지/직링크 PDF | 낮음 | 낮음 | 낮음 |
| 실행-4(수동) | VBA 매크로 WinHttp 다운로드 | 중간 | 낮음~중간 | 중간(실행-2와 동일 사유) |

---

## 3. 원격 연결/백도어 가능성 점검 (2026-09-11)

코드 전체(`.venv` 제외)를 아래 패턴으로 grep해 확인. **전부 해당 없음**.

| 확인 항목 | 결과 |
|---|---|
| 서버 소켓/웹서버(`socket.bind`/`.listen`, Flask, FastAPI, uvicorn, http.server 등) | 없음 |
| 원격 제어 도구 흔적(VNC/TeamViewer/역방향 쉘) | 없음 |
| 텔레메트리/사용현황 전송(beacon, analytics, phone-home) | 없음 |
| Windows 시작프로그램 등록(레지스트리 Run 키) | 없음(`installer/setup.iss` 확인 — 바로가기만 생성, Run 키 없음) |
| 예약 작업(Task Scheduler) 등록 | 없음 |
| 방화벽 규칙 변경(netsh/advfirewall) | 없음 |
| 브라우저 자동화(CDP) 디버그 포트를 외부(0.0.0.0)로 개방 | 없음 — 커스텀 launch 인자 없이 기본값 사용, 기본은 루프백(127.0.0.1)에만 바인딩되고 요청 처리 후 종료 |

**결론**: 나가는 연결(outbound)만 있고 들어오는 연결(inbound/서버 역할)은 코드상 존재하지 않는다. 재부팅 시 자동 실행되거나 백그라운드에 상주하며 무언가를 전송하는 지속성(persistence) 메커니즘도 없다. "외부에서 이 프로그램에 원격으로 접속"하는 경로는 확인되지 않았다.

---

## 4. 종합 — 우선순위별 권고

1. **[저작권/라이선스, 중간~높음] `pymupdf`(AGPL) 라이선스 검토** — 무료로 배포되는 Setup.exe가 소스 비공개 전제라면 AGPL 조건과 충돌 가능. 대안: Artifex 상용 라이선스 구매, 또는 PDF 처리를 `pypdfium2`/`pdfminer.six`(permissive 라이선스)로 교체 검토.
2. **[해킹 노출/불법 프로그램, 높음] 임의 도메인 PDF 다운로드에 최소한의 안전장치 추가** — 예: 알려진 악성/피싱 도메인 차단리스트 대조, 확장자·크기 상한, PDF 내부 JS/액션 스캔(현재는 `%PDF` 매직바이트 확인이 전부).
3. **[해킹 노출, 중간] 다운로드물 무결성 검증 부재** — Python 설치기(번들)·Chrome 설치기·pip 패키지 어디에도 해시/서명 검증이 없음. 최소한 `requirements.txt` 버전 고정 + `pip install --require-hashes` 도입 권장.
4. **[투명성, 중간] Chrome 무음 자동 설치를 별도 동의 단계로 분리** — 지금은 전체 "지금 실행" 동의에 뭉뚱그려짐.
5. **[정보성] 원격 연결/백도어**: 점검 결과 이상 없음 — 추가 조치 불필요.

## 5. 이번 점검에서 제외한 것 (다음 라운드 후보)

- Mouser API 키 저장/전송 방식 보안(`User_API/` 폴더, 평문 저장 여부 등) — 사용자가 명시적으로 이번 범위에서 제외함.
- `.bas` 매크로(`vba/datasheet_helper.bas`) 자체의 코드 인젝션/매크로 보안 심층 검토.
- 다운로드된 PDF를 여는 뷰어(PyMuPDF/외부 프로그램) 자체의 알려진 CVE 여부.

---

## 6. 저작권 침해 요소 후속 점검 (2026-09-15)

`.gitignore` 강화 작업 중, 예시 데이터시트 8개 파일(`IS31FL3296-UTLS4-TR_예시.pdf`,
`IS31FL3296-UTLS4-TR_예시안.pdf`, `Data_list_217F_예시.xlsx`, `Data_list_217F_클로드분석.xlsx`,
`Data_list_217F_파라미터예시.xlsx`, `클로드 학습자료/` 폴더 안 3개 파일)가 **이미 git에
추적(tracked)돼 있고, 커밋 `840ea19`를 통해 공개 GitHub 저장소(origin/master)에 푸시된 상태임을
확인함** — 앞선 확인(2026-09-11, 이 문서 §1)에서 "커밋된 적 없다"고 한 것은 확인 스크립트의
한글 인코딩 처리 버그로 인한 오판이었음, 정정함.

- **상황**: 위 8개 파일 제거 범위를 "가벼운 조치"(`git rm --cached`, 최신 스냅샷에서만 제거·과거
  커밋엔 흔적 남음)와 "완전한 조치"(`git filter-repo` + 강제 푸시, 과거 커밋에서도 제거하되
  되돌리기 어려움) 두 가지로 나눠 설명하고 사용자에게 선택하도록 함. 이후 사용자가 "지금
  삭제하려는 게 PDF인지 부품리스트 시트의 품번인지" 정확히 구분해달라고 재질문함 — 실제
  xlsx 4개(`Data_list_217F_예시.xlsx`/`_클로드분석.xlsx`/`_파라미터예시.xlsx`,
  `클로드 학습자료/SI53340-B-GM_클로드분석.xlsx`)를 openpyxl로 직접 열어 확인한 결과, `부품리스트`
  시트에 품번·로컬 파일 경로 텍스트만 있을 뿐 **PDF 원본 내용(제조사 저작권 표현물)은 전혀
  포함돼 있지 않음**을 확인함 — 저작권 문제의 실체는 PDF 4개뿐임이 명확해짐.
- **결정(최종, 2026-09-15)**: **PDF 4개는 완전한 조치**(`git filter-repo` + 강제 푸시)로 모든
  과거 커밋에서 제거. **xlsx 4개(부품리스트 품번 포함)는 삭제하지 않고 그대로 기록으로 유지**한다
  (사용자 확정) — 품번 자체는 저작권 보호 대상이 아닌 사실(fact)이고, 데이터시트 분석 규칙을
  다지는 이 프로젝트의 학습/참고 목적에 필요하다고 판단.
- **반영**:
  - PDF 4개(`IS31FL3296-UTLS4-TR_예시.pdf`, `IS31FL3296-UTLS4-TR_예시안.pdf`,
    `클로드 학습자료/IS31FL3296-UTLS4-TR_클로드분석.pdf`,
    `클로드 학습자료/SI53340-B-GM_클로드분석.pdf`)를 `git-filter-repo --invert-paths`로 42개 커밋
    전체에서 제거(로컬 디스크 사본도 함께 사라짐 — filter-repo가 워킹트리를 새 HEAD 기준으로
    다시 씀). 원격 `origin/master`에 강제 푸시(구 HEAD `d8a8099` → 신 HEAD `2048ece`).
  - 원격에 있던 GitHub Release 태그 `lite-v1.1.0`(구 커밋 `c7b1520`, PDF 도입 커밋 `840ea19`의
    후손이라 PDF가 그대로 포함돼 있었음)도 새 히스토리의 대응 커밋(`02f5b1c`)으로 옮겨 강제
    푸시함 — 태그 하나만 놓쳤으면 master를 아무리 강제 푸시해도 그 태그를 통해 PDF가 계속
    접근 가능했을 것. `git ls-remote origin`으로 브랜치/태그 전수 확인 후 진행, 재작성 후 다시
    확인해 PDF가 어느 ref에서도 안 나오는 것 확인함.
  - xlsx 4개는 filter-repo 대상에서 제외해 그대로 보존(품번·분석 기록 그대로 남음).
  - `git-filter-repo`는 이번 작업을 위해 `.venv`에 pip로 설치함(일회성 도구, `requirements.txt`에는
    안 넣음 — 배포되는 앱과 무관).
  - **잔존 참고사항**: 강제 푸시 이전에 이 저장소를 이미 클론/포크한 사람이 있다면 그 사람의
    로컬 사본에는 예전 히스토리(PDF 포함)가 그대로 남아있음 — git 히스토리 재작성은 새로 클론하는
    사람부터만 효과가 있음. GitHub 쪽 캐시(예: 특정 blob SHA 직접 링크)도 완전히 사라지기까지
    약간의 시차가 있을 수 있음.

### 우선순위 권고 갱신

- §4의 항목 4(Chrome 무음 설치 투명성)는 그대로 유효.
- **완료.** [라이선스, 낮음] `PySide6`(LGPL) 재배포 조건 확인 — `THIRD-PARTY-NOTICES.md` 동봉 +
  `setup.iss`(원본/Lite) 시작메뉴 바로가기 추가로 반영 완료.
- **완료.** [라이선스, 낮음] pip 패키지 40종 전체 라이선스 고지문 생성 — `THIRD-PARTY-NOTICES.md`
  (프로젝트 루트, 2026-09-15). pymupdf(AGPL)·PySide6(LGPL)·tld(3중 라이선스) 확인사항 별도 강조.
- **완료.** 예시 데이터시트 PDF 4종의 공개 저장소 노출 — `git filter-repo` + 강제 푸시로 히스토리
  완전 제거(위 참고). 부품리스트 품번만 담은 xlsx 4종은 저작권 대상이 아니라 그대로 유지.

### 2026-09-15 (계속) — 남은 저작권 항목 3개 중 2개 종결

- **상황**: 앞선 정리에서 저작권 관련 미해결 항목이 4개(①`pymupdf` AGPL, ⑤임의 도메인 애그리게이터
  다운로드, ⑥PDF 주석 2차적 저작물, ⑧사내 문서 공개 노출 의심)로 압축됨.
- **결정**:
  - **⑤ 임의 도메인(애그리게이터) 데이터시트 다운로드 — 현상 유지**(사용자 확정). 코드/정책
    변경 없이 현재 상태(공식 도메인 우선순위만 있고 애그리게이터 차단 없음) 그대로 감수한다.
  - **⑥ PDF 주석(하이라이트+메모)이 2차적 저작물이라는 우려 — 문제없음으로 종결**(사용자 확정).
    주석 대상 데이터시트 자체가 제조사가 엔지니어링 참고용으로 공식 배포하는 자료이므로, 내부
    분석용으로 주석을 다는 것은 통상적 이용 범위로 판단.
  - **⑧ `Windchill_217F_Mapping_Template.xlsx`/`Data_list_217F.xlsx`의 사내 문서 노출 의심 —
    문제없음으로 종결**(사용자 확정). 사내 기밀 문서가 아니라 공식(공개) 자료 기반이므로 저장소에
    있어도 무방함.
- **반영**: 코드 변경 없음(둘 다 판단 종결, 하나는 리스크 감수). 저작권 항목 목록에서 ⑥·⑧ 삭제,
  ⑤는 "현상 유지"로 표시. **남은 저작권 항목은 `pymupdf`(AGPL) 하나뿐.**

### `THIRD-PARTY-NOTICES.md` 재생성 절차

`requirements.txt`(직접 의존성 7종)가 바뀌면 하위 의존성도 달라지므로 다시 생성해야 한다.

```bash
.venv/Scripts/python.exe -m pip show <패키지1> <패키지2> ...   # 개별 확인용
```

또는 `.venv`에 설치된 전체 목록으로 일괄 생성하려면 `importlib.metadata`로 각 패키지의
`License-Expression`/`License`/`Home-page`를 순회해 표로 뽑으면 된다(이번에 실제로 쓴 방식 —
2026-09-15 생성 스크립트가 이 파일 자체는 남기지 않았으니, 필요하면 `pip list --format=freeze`로
설치 목록부터 다시 뽑아 같은 방식을 반복할 것). `pymupdf`/`PySide6`/`tld`처럼 카피레프트 라이선스가
새로 섞여 들어오는지는 매번 눈으로 한 번 더 확인.
