# 작업 인계 문서 (Handoff) — 2026-09-20

> 다른 환경에서 이어서 작업하기 위한 인계용. (이전 문서: `HANDOFF_2026-09-15.md`)
> 프로젝트: 데이터시트 자동분석 → PTC Windchill(MIL-HDBK-217F Notice 2) 신뢰도 파라미터 생성.
>
> 이번 문서는 **다운로드 기능 확장(DigiKey API 추가) + 실전 로그 기반 성능 개선**을 다룬다.
> 관련 커밋: `f955d95`(설계 문서) → `9a38c32`(DigiKey 구현) → `9889d51`(성능 버그 수정).

---

## 0. 가장 큰 변화

이번 세션(2026-09-19 ~ 09-20)은 크게 세 갈래로 진행됨:

1. **코드베이스 전체 학습** — 문서(`CLAUDE.md`/`소프트웨어_설계문서.md`/최신 핸드오프)와 소스코드
   전체를 정독. 메모리에 남아있던 "설계문서가 낡았다"는 옛 기록이 이미 2026-09-11 세션에서
   해결된 걸 확인하고 메모리도 최신화함(코드 변경 없음).
2. **DigiKey Product Information API v4를 두 번째 다운로드 경로로 추가** — Mouser 실패 후,
   DuckDuckGo 웹검색 전에 끼워 넣는 보조 경로. 설계→실제 라이브 API 호출로 검증→구현까지 완료.
3. **실전 다운로드 로그 분석으로 성능 버그 2건 발견/수정** — 22개 품번 배치가 4분36초 걸렸던
   실제 로그를 분석해, DigiKey URL 버그와 과도한 재시도가 원인이었음을 찾아 고침. 사용자가
   재실행해서 "확실히 시간을 절약할 수 있었다"고 효과 확인함.

**아직 완료되지 않은 것**: API 일일 사용량 추적 기능(4번 참고)은 브레인스토밍 1단계(표시 방식
질문 하나)까지만 진행되고 사용자가 중단시킴 — **설계도 코드도 아직 없음**, 다음 세션에서 이어갈 것.

---

## 1. DigiKey Product Information API v4 다운로드 경로 추가 (2026-09-19)

### 1-1. 배경
기존엔 Mouser API가 실패하면 곧바로 DuckDuckGo 웹검색(브라우저 자동화, 느리고 과거 IP 차단
이력 있음)으로 넘어갔음. 사용자가 DigiKey Client ID/Secret(Production 승인 완료)을 발급받아
`User_API/JY_DIGIKEY_APT_KEY.txt`에 저장해둔 상태였음.

### 1-2. 설계 과정
`brainstorming` 스킬로 진행 — DigiKey OAuth2 사양(`client_credentials`, 2-legged)과 키워드 검색
API를 웹 조사로 먼저 확인한 뒤, 승인 상태(Production)와 API 키 파일 형식(관례에 맞춰
`DIGIKEY_CLIENT_ID`/`DIGIKEY_CLIENT_SECRET`로 통일)을 사용자에게 확인. 설계 문서를
[`docs/superpowers/specs/2026-09-19-digikey-api-download-design.md`](docs/superpowers/specs/2026-09-19-digikey-api-download-design.md)에
작성·커밋(`f955d95`) 후 사용자 승인받고 구현(`9a38c32`).

### 1-3. 구현 내용
| 파일 | 변경 |
|---|---|
| `datasheet/digikey_search.py` (신규) | `DigiKeyClient` — OAuth2 토큰 발급/캐시(10분 TTL, 만료 30초 전 자동 재발급), 키워드 검색(`search_part()`), `MouserClient.search_part()`와 같은 반환 형태 |
| `datasheet/search.py` | `_normalize_part_number` → `normalize_part_number`로 공개(DigiKey 클라이언트와 공유) |
| `datasheet/downloader.py` | `_download_via_mouser_url` → `_download_via_api_source_url`로 일반화(Mouser/DigiKey 공용), `download_datasheet_for_part()`에 **②-2 DigiKey 단계**를 Mouser 실패 후·DDG 전에 삽입 |
| `utils/config.py` | `get_digikey_client_id()`/`get_digikey_client_secret()`, `STATUS_SUCCESS_DIGIKEY` 추가 |
| `ui/main_window.py` | `DownloadWorker`가 배치 시작 시 `DigiKeyClient`도 생성(키 없으면 로그만 남기고 `None`으로 우아하게 건너뜀 — Mouser와 달리 필수 아님), `SUCCESS_STATUSES`에 반영 |
| `User_API/JY_DIGIKEY_APT_KEY.txt` | 키 이름을 `Client ID`/`Client Secret` → `DIGIKEY_CLIENT_ID`/`DIGIKEY_CLIENT_SECRET`로 정리(gitignore 대상, 값 자체는 안 바뀜) |
| `소프트웨어_설계문서.md` | §2 폴더구조, §3.2 다운로드 흐름에 DigiKey 단계 서술 추가 |

### 1-4. 검증
라이브 API 호출로 직접 확인(테스트 프레임워크 없이 실제 네트워크 호출로 검증):
- OAuth2 토큰 발급 성공
- 실제 품번 2건(`SMW3100RJT`, `T322D106K035AT`) 검색 → 제조사/데이터시트 URL 정확
- 경량(`requests`) 다운로드로 실제 PDF 987KB 수신, `%PDF` 매직바이트 확인
- Mouser 실패(mock) → DigiKey 성공 전체 경로, `STATUS_SUCCESS_DIGIKEY` 정상 반환
- 우아한 성능저하 3종(키 없음/빈 응답/예외) 모두 배치를 막지 않고 다음 단계로 진행 확인

---

## 2. 실전 로그 기반 성능 버그 수정 (2026-09-19~20)

사용자가 22개 품번 실제 다운로드 로그(4분36초, 성공 14/실패 8)를 제공해서 분석함.

### 2-1. 발견한 문제 2가지
1. **[확정 버그] DigiKey `DatasheetUrl`이 가끔 프로토콜 없는 `//mm.digikey.com/...` 형태로 옴** —
   `requests`가 "Invalid URL: No scheme supplied"로 즉시 거부하는데, 이 실패가 "일시적 오류"로
   분류돼 브라우저 재시도(지수 백오프 3회)까지 타면서 **절대 성공 못 할 요청에 품번 하나당
   최대 2분 이상** 날림(`MT41K128M16JT-125:K`/`AO3400A`/`MCR12DSNT4G` 3건에서 발생, 그중 2건이
   겹쳐서 배치 마지막 2분을 통째로 날림 — 전체 실행시간의 40% 이상).
2. **[튜닝] 공식 링크(Mouser/DigiKey) 브라우저 폴백이 기본 3회 재시도** — 연결 자체가 안 되는
   죽은 호스트(`samsungsem.com`, `productfinder.pulseelectronics.com` 등)엔 재시도해도 똑같이
   실패할 뿐이라 시간 낭비.

### 2-2. 수정 (커밋 `9889d51`)
- `datasheet/digikey_search.py`: `_normalize_datasheet_url()` 추가 — `//`로 시작하면 `https:`
  붙임. `search_part()`가 반환하기 직전에 적용.
- `datasheet/downloader.py`: `_download_via_api_source_url()`의 브라우저 폴백을
  `download_pdf(url, dest, max_retries=1)`로 호출(기본값 3 대신) — 죽은 호스트 최악 시나리오
  ~130초 → ~60초.
- **의도적으로 안 건드린 것**: Mouser 제품 상세페이지 스크레이핑 시도(이번 로그 9전 9패였지만,
  과거 결정로그에 "덜 막힌 사이트가 오면 도움될 수도 있다"는 이유로 의도적으로 남겨둔 경로 —
  사용자가 그대로 유지하기로 확정).

### 2-3. 검증
- `MT41K128M16JT-125:K`(버그로 실패했던 품번)를 실제로 재현 다운로드 → 수정 후 **3초 만에 성공**
  (5.2MB PDF, `%PDF` 확인). 수정 전이었다면 2분+ 걸려 실패했을 요청.
- `max_retries` 축소 효과를 mock으로 순수 대기시간만 측정: 14초(3회) → 2초(1회).
- **사용자가 실제로 재실행해서 "확실히 시간을 절약할 수 있었다"고 효과 확인함**(정확한 수치는
  아직 로그로 재확인 안 됨 — 다음 배치 로그가 있으면 함께 기록할 것).

---

## 3. 다른 환경에서 셋업 시 주의

`HANDOFF_2026-09-15.md`와 대부분 동일. 추가된 부분만:

1. **`User_API/`에 DigiKey 키가 필요하면** `DIGIKEY_CLIENT_ID=...`/`DIGIKEY_CLIENT_SECRET=...`
   형식으로 파일 하나(또는 두 줄 다 한 파일)를 넣을 것 — 없어도 프로그램은 정상 동작하며
   DigiKey 단계만 조용히 건너뜀(Mouser는 필수, DigiKey는 선택).
2. DigiKey 앱은 **Production 승인**이 돼 있어야 실제 데이터가 나온다(승인 전엔
   `sandbox-api.digikey.com`만 되고 고정 더미 데이터만 돌아옴 — 이 프로젝트가 쓰는 키는
   Production 승인 완료 상태, 2026-09-19 사용자 확인).
3. 새 의존성은 없음(`requests`는 기존에 이미 있었음).

---

## 4. 남은 작업 (TODO)

1. **API 일일 사용량 추적 기능 — 설계 중단 상태(코드/스펙 문서 없음)**. 조사까지는 끝남:
   - Mouser Search API: 30건/분, **1,000건/일**(공식 문서 확인) — 응답 헤더에 사용량 정보
     없음(직접 호출해서 확인함) + 웹사이트(My Mouser Account/API Explorer)도 봇 차단이라
     자동 조회 불가 → **우리 프로그램이 보낸 호출 수를 직접 로컬에서 세는 게 유일한 방법**.
   - DigiKey Product Information API v4: 120건/분, **1,000건/일**(공식 문서 확인) — **매 검색
     응답 헤더에 `x-ratelimit-limit`/`x-ratelimit-remaining`이 실시간으로 찍힘**(서버측 정확한
     값, 별도 추적 불필요 — 실제 호출로 `1000`/`950` 확인함).
   - 브레인스토밍 1번째 질문("사용량을 어디서 확인하고 싶은지")에서 사용자가 **"메인 창에 항상
     보이는 라벨"**을 선택한 직후 중단됨 — 다음 세션에서 이 답변부터 이어서 설계를 마무리할 것.
   - 구현 시 참고할 지점: 호출 카운트는 실제 HTTP 요청이 나가는 지점(`datasheet/search.py`의
     `MouserClient._search_once()`, `datasheet/digikey_search.py`의 `DigiKeyClient._search_once()`)
     에서 세야 함 — `download_datasheet_for_part()` 호출 횟수(=품번 수)와 다름(정규화 재시도로
     품번 하나당 API 호출이 2번까지 날 수 있음).
2. (이전 문서에서 이어짐, 아직 미착수) pip 패키지 버전 고정 + 해시 검증, 임의 도메인(애그리게이터)
   PDF 다운로드 화이트리스트, Chrome 무음 자동 설치 투명성, `diagnostics/self_check.py`를 PSA
   구조에 맞게 재작성.

---

## 5. 더 보기

- [`docs/superpowers/specs/2026-09-19-digikey-api-download-design.md`](docs/superpowers/specs/2026-09-19-digikey-api-download-design.md) — DigiKey 통합 설계 문서.
- [`소프트웨어_설계문서.md`](소프트웨어_설계문서.md) §3.2 — 다운로드 흐름 최신 구현(DigiKey 단계 + 이번 성능 수정 포함).
- [`HANDOFF_2026-09-15.md`](HANDOFF_2026-09-15.md) — 이전 인계 문서(보안/저작권 감사).
