# 소프트웨어 설계문서 — 전자부품 데이터시트 수집·분석 프로그램

- 대상 프로그램: `데이터시트_다운로더` (Python, PySide6 GUI)
- 관련 문서: [`설계서.txt`](../설계서.txt)(요구사항 정의), [`CLAUDE.md`](CLAUDE.md)(217F 분석 규칙·색상 규칙 상세)
- 이 문서의 범위: 위 두 문서가 정한 요구사항을 실제로 어떤 모듈/함수로 구현했는지 정리한 **구현 설계문서**.

---

## 1. 개요

인터넷에서 전자부품 데이터시트(PDF)를 검색·다운로드하고, MIL-HDBK-217F Notice 2 기준으로 신뢰도 분석
파라미터를 추출해 PTC Windchill Quality에 임포트할 수 있는 엑셀(출력지)과, 값의 근거를 알 수 있도록
주석을 삽입한 PDF를 만들어내는 데스크톱 프로그램이다.

전체 처리 순서:

```
입력지(엑셀) 로딩
   ↓
품목번호+제조사로 데이터시트 검색·다운로드 (Mouser API → 웹검색 순)
   ↓
규칙 기반 분석 (대분류/소분류 판별 + 파라미터 값 추출)
   ↓
사람 검토 (GUI 다이얼로그에서 확인/수정, 확신도별 색 표시)
   ↓
출력지 생성 (부품리스트 + 매핑맵 시트를 담은 새 엑셀)
   ↓
데이터시트 PDF에 스티키노트 주석 삽입
```

> 자가진단(`diagnostics/self_check.py`)은 이 사용자 흐름에 포함되지 않는다. GUI에 노출되지 않는
> 개발자용 내부 점검 도구이며, 자세한 내용은 3.7절 참고.

---

## 2. 폴더 구조

```
데이터시트_다운로더/
├── main.py                     # 진입점 (QApplication 생성)
├── ui/                         # 화면
│   ├── main_window.py          # 메인 창 + 다운로드/분석 워커 스레드
│   ├── analysis_dialog.py      # 분석 결과 검토 다이얼로그
│   └── dialogs.py              # 시트/컬럼 수동 선택 팝업
├── datasheet/                  # 검색 · 다운로드 · 주석
│   ├── search.py               # Mouser API 클라이언트
│   ├── downloader.py           # Mouser → 웹검색(DuckDuckGo) 순 다운로드
│   └── annotator.py            # PDF 스티키노트 주석 삽입 (PyMuPDF)
├── ai/                         # 217F 규칙 기반 분석
│   ├── classifier.py           # 대분류/소분류 판별 (키워드 매칭)
│   ├── field_extractor.py      # 파라미터 값 추출 (동의어+숫자패턴)
│   ├── pdf_text.py             # PDF → 텍스트 추출 (pdfplumber)
│   ├── pdf_parser.py           # 위 세 개를 묶은 analyze_pdf()
│   ├── prompt.py               # data/*.json 로더, 색상별 필드 집합, AI 프롬프트 템플릿
│   └── analysis_state.py       # PartAnalysis 데이터 모델
├── excel/                      # 엑셀 입출력
│   ├── excel_reader.py         # 입력지 로딩
│   ├── excel_writer.py         # 입력지에 처리 상태 체크포인트 기록
│   ├── mapping_colors.py       # 매핑맵 4색 규칙 상수
│   ├── mapping_template_builder.py  # data/*.json으로 빈 매핑맵 시트 생성
│   ├── mapping_writer.py       # 매핑맵에 부품 값 쓰기(행 찾기/복제 포함)
│   └── output_builder.py       # 출력지(3시트) 조립
├── diagnostics/
│   └── self_check.py           # 자가진단 4종 (개발자용 내부 도구, GUI 미연동)
├── utils/
│   ├── config.py                # 경로/상태 문자열/컬럼명 등 공용 설정
│   └── logger.py                # 파일+GUI 로그
└── data/                        # 검증된 참고 데이터 (재추론 금지, CLAUDE.md 4번)
    ├── headers.json             # 매핑맵 127개 컬럼 헤더
    ├── subcat_params.json       # 154개 서브카테고리별 적용 파라미터
    └── params124.json           # 파라미터 설명(일부)
```

---

## 3. 데이터 흐름 상세

### 3.1 입력지 로딩
`excel_reader.read_part_list_sheet()`가 '부품리스트' 시트와 품번/제조사 컬럼을 자동 인식한다.
실패하면 `ui/dialogs.SheetColumnDialog`로 사용자가 직접 시트·컬럼을 고른다.

### 3.2 다운로드
`ui/main_window.DatasheetWorker`가 별도 스레드에서 `datasheet.downloader.download_datasheet_for_part()`를
행마다 호출한다. 순서: ① 이미 저장된 파일 있으면 스킵 → ② `MouserClient.search_part()` → ③ 실패 시
`find_datasheet()`(DuckDuckGo 웹검색, 제조사 공식 도메인 우선) → ④ 그래도 없으면 실패 처리.

### 3.3 분석
다운로드 성공 시 같은 워커가 `ai.pdf_parser.analyze_pdf()`를 호출한다.
- `pdf_text.extract_text()`로 앞 4페이지 텍스트 추출
- `classifier.classify()`로 대분류/소분류 + 확신도 판별 (키워드 가중치 합산)
- `prompt.get_fields_for_subcategory()`로 그 서브카테고리의 흰 칸(직접 입력 대상) 목록 조회
- `field_extractor.extract_field_values()`로 각 필드 값을 "동의어 근처 숫자+단위" 패턴으로 추출

결과는 `ai.analysis_state.PartAnalysis`(category, subcategory, 확신도, fields, confirmed_fields)로
감싸 행 인덱스별로 `MainWindow.analysis` 리스트에 보관한다. 대분류/소분류를 못 정하면 `분류불가`,
정했으면 (필드가 다 채워졌어도) 사람이 아직 확인 안 했으므로 항상 `검토필요` 상태로 시작한다.

### 3.4 검토 (사람 확인)
표의 "검토" 버튼 → `ui.analysis_dialog.AnalysisReviewDialog`.
- 대분류/소분류를 드롭다운으로 재지정 가능(바꾸면 필드 목록 재계산)
- 필드별 값 편집 + "확인" 체크박스
- 행 색: 🟥 값 없음 / 🟨 자동추출·미확인 / 🟩 확인됨
- 남은 미확인 필드 이름은 "미확인 항목" 컬럼에 기록되고, 입력 엑셀에도 체크포인트로 저장됨

### 3.5 출력지 생성
`excel.output_builder.build_output_workbook()`이 실행마다 새 엑셀을 만든다 (원본 입력 파일·마스터
템플릿은 절대 수정하지 않음).
1. **사용가이드라인** 시트 — 마스터 파일(`utils.config.MAPPING_TEMPLATE_PATH`)에서 값+서식을 읽기만
   해서 복사 (마스터는 저장하지 않음).
2. **부품리스트** 시트 — 이번 배치의 입력지 내용 + 처리 결과.
3. **매핑맵** 시트 — `mapping_template_builder.build_blank_mapping_sheet()`로 `data/headers.json` +
   `data/subcat_params.json` + `ai.prompt`의 보라/분홍 필드 집합만으로 154개 서브카테고리 빈 행을
   색칠해 새로 생성한 뒤, 기존 `mapping_writer.find_or_create_row()`/`write_part_to_mapping()`을
   그대로 재사용해 이번 배치 부품 값을 채운다(같은 서브카테고리 중복 시 서식 복제 포함).

### 3.6 PDF 주석
`datasheet.annotator.annotate_pdf()`(PyMuPDF)가 값이 있는 필드마다 PDF 본문에서 그 값 문자열을
`page.search_for()`로 찾아 그 위치에 스티키노트를 삽입한다. 위치를 못 찾은 값은 1페이지 요약 주석
하나로 모아둔다. **분류/추출이 아예 실패해 채울 값이 하나도 없어도**, 다운로드된 부품은 산출물에서
항상 확인할 수 있어야 하므로 "자동 분석 결과 없음" 요약 주석을 남긴다 — 이 경우가 없으면 분석에
실패한 부품의 PDF가 배치 산출물에서 조용히 빠지게 된다. 출력지 엑셀 옆 `<출력파일명>_datasheets/`
폴더에 저장되며, 원본 다운로드 PDF는 건드리지 않는다. (참고: PDF가 아예 다운로드되지 않은 부품은
주석을 붙일 대상 자체가 없으므로 산출물에도 나타나지 않는다 — 이 경우는 다운로드 실패이지 주석
기능의 문제가 아니다.)

### 3.7 자가진단 (개발자용 내부 도구 — GUI에 노출되지 않음)
`diagnostics/self_check.py`는 **사용자 기능이 아니라, 코드를 개발/수정할 때 안정성을 확인하기 위한
내부 점검 도구**다. GUI에 버튼이나 메뉴로 연결되어 있지 않으며, 필요할 때 개발자가 파이썬
콘솔이나 스크립트에서 직접 호출해서 쓴다.

| 함수 | 점검 내용 |
|---|---|
| `check_workbook_integrity()` | 매핑맵 헤더 일치 여부, 회/보라/분홍 칸이 기대한 색과 같은지 (흰색은 "칠 없음"으로도 정상 표현되므로 검사 제외) |
| `check_connectivity()` | Mouser API 키/응답, 인터넷 연결, 다운로드 폴더 쓰기 권한 |
| `check_field_anomalies()` | 숫자형 필드의 음수·단위누락·상식 밖 범위 |
| `check_output_file()` | 방금 만든 엑셀/PDF를 다시 열어 무결성 확인 |

---

## 4. 핵심 데이터 모델

### `ai/analysis_state.py` — `PartAnalysis`
```python
category: str | None
category_confidence: int
subcategory: str | None
subcategory_confidence: int
fields: dict[str, str | None]       # 파라미터명 -> 추출된 값(또는 None)
confirmed_fields: set[str]          # 사람이 확인한 필드 이름들
```
`unresolved_field_names()` / `is_fully_confirmed()`로 미확인 항목과 완료 여부를 판단한다.

### `excel/mapping_colors.py` — 4색 규칙
| 색 | HEX | 의미 |
|---|---|---|
| 흰색 | `FFFFFF` | 직접 입력 |
| 회색 | `D9D9D9` | 해당 없음(N/A), 값 넣지 않음 |
| 보라 | `D9C6EC` | PTC 자동결정, 값 넣지 않음 |
| 분홍 | `F8CBAD` | 데이터시트로 알 수 없음, 항상 공란 |

`mapping_template_builder.expected_hex_for()`가 이 규칙을 코드로 구현하며, 매핑맵을 **생성**할 때와
자가진단으로 **검증**할 때 동일한 함수를 재사용해 두 로직이 어긋나지 않게 한다.

### `data/*.json`
CLAUDE.md에 따라 이미 검증된 데이터이므로 재추론하지 않고 그대로 불러다 쓴다(`ai/prompt.py`).

---

## 5. 기능별 구현 현황 (`설계서.txt` 대비)

| # | 기능 | 상태 | 구현 위치 |
|---|---|---|---|
| 1 | 입력지 작성(엑셀, 순번/품목번호/제조사) | ✅ 완료 | `excel/excel_reader.py`, `ui/dialogs.py` |
| 2 | 인터넷 검색 및 다운로드 | ✅ 완료 | `datasheet/search.py`, `datasheet/downloader.py` |
| 3 | 데이터 분석(217F 기준, 규칙 기반) | ✅ 완료 | `ai/classifier.py`, `ai/field_extractor.py`, `ai/pdf_parser.py` |
| 3-a | 사람 검토(GUI 표 + 엑셀 미확인 항목) | ✅ 완료 | `ui/analysis_dialog.py`, `ui/main_window.py` |
| 4 | 파라미터 작성(출력지 생성, 배치마다 신규) | ✅ 완료 | `excel/mapping_template_builder.py`, `excel/output_builder.py` |
| 5 | 데이터시트(PDF)에 주석 작성 | ✅ 완료 | `datasheet/annotator.py` |
| 6 | 출력(엑셀 + 주석 PDF 2종) | ✅ 완료 | `ui/main_window._export_to_excel()` |
| 부가 1 | Python(PySide6) 구현 | ✅ 완료 | 전체 |
| 부가 2 | 자가 검진 코드 (개발자용 내부 도구, GUI 미노출) | ✅ 완료 | `diagnostics/self_check.py` |

---

## 5-1. 자동 다운로드가 막힌 부품 — VBA 다운로드 도우미

일부 제조사/유통사 사이트(analog.com, mouser.com 등)는 Akamai 기반 봇 차단으로, 클라우드/자동화
환경에서 나가는 요청을 IP 단위로 차단한다(실제로 curl_cffi 요청과 Claude의 실제 브라우저 도구
양쪽 모두 동일한 403으로 막히는 것을 확인함 — 헤더/브라우저 종류의 문제가 아니라 요청 IP 자체의
문제로 판단됨).

- 자동 다운로드가 실패해도, 웹검색으로 찾아낸 링크가 있으면 부품리스트 시트의 **"참고 링크"**
  칸(클릭 가능한 하이퍼링크)과 **"저장 경로"** 칸(이 PDF가 있어야 할 정확한 위치)에 기록된다
  (`ui/main_window.py`, `excel/output_builder.py`).
- [`vba/datasheet_helper.bas`](vba/datasheet_helper.bas): 출력지 엑셀에서 실행하는 보조 매크로.
  "다운로드 상태"가 "실패"인 행마다 ① `URLDownloadToFile`로 직접 받아보고, ② 받은 파일이 진짜
  PDF인지 확인(맨 앞 4바이트가 `%PDF`인지) 후, 아니면 차단 페이지로 간주해 지우고 기본
  브라우저로 그 링크를 열어 사람이 직접 저장하게 한다. 사용자의 실제 네트워크에서 실행되므로
  이 프로그램(클라우드 환경)에서와 다른 결과가 나올 수 있다.
- **검증 범위**: 이 매크로가 만들어내는 엑셀 컬럼 구조(품번/다운로드 상태/참고 링크/저장 경로)와
  Python 쪽 파이프라인은 실제 실패 케이스(AD8030ARZ)로 끝까지 검증했다. 다만 VBA 코드 자체의
  실행은 Excel/VBA 런타임이 없는 환경에서는 검증할 수 없어, 사용자가 직접 실행해 확인해야 한다.

## 6. 알려진 이슈 / 참고사항

- **마스터 템플릿 파일 손상**: 개발 중 `check_workbook_integrity()`를 직접 호출해 확인한 결과,
  `Windchill_217F_Mapping_Template.xlsx`(복사본이 아닌 원본 파일명)는 이미 색상 서식과 기존 6개
  부품 데이터가 전부 사라진 상태였다(CLAUDE.md가 경고하는 "LibreOffice 재저장 색상 소실 사고"와 동일
  증상). 같은 폴더의 `Windchill_217F_Mapping_Template - 복사본.xlsx`는 온전하므로, 프로그램의
  `utils.config.MAPPING_TEMPLATE_PATH`는 이 백업 파일을 가리키도록 설정했다. 손상된 원본 파일은
  건드리지 않았으니, 필요시 직접 확인/정리가 필요하다.
- **규칙 기반 분석의 한계**: 대분류/소분류 판별과 파라미터 추출은 키워드/정규식 기반 best-effort이며,
  데이터시트마다 표현이 달라 정확도가 낮을 수 있다(코드 주석에도 명시). 그래서 모든 자동 추출값은
  검토 다이얼로그에서 사람이 확인하기 전까지 "검토필요" 상태로 남는다.
- **다운로드 실패는 주석 기능과 무관**: "출력" 후 `_datasheets` 폴더에 파일이 안 보인다면, 먼저
  "다운로드 상태" 컬럼을 확인해야 한다. 다운로드 자체가 실패한 부품(Mouser/인터넷 접근 문제 등)은
  주석을 붙일 PDF가 없어 산출물에도 나타나지 않는다 — 이는 정상 동작이며 분석/주석 로직의 결함이
  아니다. 반대로 다운로드는 됐지만 분류/추출이 실패한 부품은 (수정 후) "자동 분석 결과 없음" 주석이
  달린 PDF가 항상 나온다.

## 7. 향후 개선 여지 (참고용, 미착수)

- 규칙 기반 분석을 LLM 기반으로 보강(설계서 인터뷰에서는 이번 범위에서 제외하기로 결정)
- 배치가 누적되면 `check_field_anomalies()`를 배치 간 통계 비교 방식으로 확장
- 손상된 마스터 원본 파일 정리/복구
