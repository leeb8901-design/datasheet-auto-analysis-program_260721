# 비밀 값 (API 키) — 절대 커밋하지 않음
.env
User_API/

# 파이썬
__pycache__/
*.pyc
.venv/

# 에디터 설정
.vscode/
.pytest_cache/

# 다운로드된 데이터시트/로그 (사용자 로컬 산출물, 코드 아님)
Download_ datasheets/
logs/

# 루트의 Import 양식은 사용자의 개인 테스트/작업용 사본 (사용자 로컬 산출물, 코드 아님).
# vba/Import_양식.xlsm(마스터 템플릿)은 계속 추적함 - 이건 루트 사본만 제외.
/Import_양식.xlsm
/Import_양식_출력.xlsx

# 원본 첨부파일 (내용은 이미 프로젝트 루트로 복사됨)
첨부파일/

# 전송/임시 산출물 (코드 아님)
repo_patch.zip
*_출력_datasheets/
*_출력.xlsx
*.backup_*.xlsx

# 예시/분석 산출물 데이터시트 (제조사 저작권 PDF 원본이 그대로 들어있을 수 있음 — 절대 커밋 금지,
# 보안점검 2026-09-11/2026-09-15 참고. 지금까지 커밋된 적은 없으나(git ls-files 확인), 실수로
# `git add -A` 했을 때 공개 저장소에 올라가는 걸 원천 차단하기 위한 명시적 규칙)
*예시*.pdf
*예시*.xlsx
*클로드분석*.pdf
*클로드분석*.xlsx
클로드 학습자료/
